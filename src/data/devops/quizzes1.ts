import type { QuizQuestion } from '../quizzes'

export const quizzes: Record<number, QuizQuestion[]> = {
  100: [
    { id: 'q100_1', question: 'Quelle distribution Linux est basee sur Debian et largement utilisee en production pour les serveurs ?', options: ['Fedora', 'Ubuntu', 'Arch Linux', 'openSUSE'], correct: 1, explanation: 'Ubuntu est basee sur Debian et est l\'une des distributions les plus populaires pour les serveurs en production grace a son cycle LTS (Long Term Support) de 5 ans et sa large communaute.' },
    { id: 'q100_2', question: 'Quel gestionnaire de paquets est utilise par defaut sur les distributions basees sur Red Hat (RHEL, Fedora, CentOS) ?', options: ['apt', 'dnf', 'pacman', 'zypper'], correct: 1, explanation: 'dnf (Dandified YUM) est le gestionnaire de paquets par defaut sur les distributions Red Hat modernes. Il a remplace yum et utilise les paquets au format .rpm.' },
    { id: 'q100_3', question: 'Quelle commande permet d\'installer un paquet sur une distribution Debian/Ubuntu ?', options: ['dnf install nginx', 'apt install nginx', 'yum install nginx', 'pacman -S nginx'], correct: 1, explanation: 'apt (Advanced Package Tool) est le gestionnaire de paquets des distributions Debian et derivees (Ubuntu, Mint). La commande "apt install" telecharge et installe le paquet ainsi que ses dependances.' },
    { id: 'q100_4', question: 'Quelle est la difference principale entre une distribution "rolling release" et une distribution a "cycle fixe" ?', options: ['Une rolling release est plus stable qu\'une distribution a cycle fixe', 'Une rolling release met a jour en continu sans versions majeures, tandis qu\'une distribution a cycle fixe publie des versions periodiques', 'Une distribution a cycle fixe ne recoit jamais de mises a jour de securite', 'Il n\'y a aucune difference fonctionnelle'], correct: 1, explanation: 'Une distribution rolling release (ex: Arch Linux) recoit des mises a jour en continu sans jamais necessiter de reinstallation. Une distribution a cycle fixe (ex: Ubuntu 22.04) publie des versions majeures a intervalles reguliers avec des mises a jour de securite entre les versions.' },
    { id: 'q100_5', question: 'Quelle commande met a jour la liste des paquets disponibles sur Ubuntu ?', options: ['apt upgrade', 'apt update', 'apt refresh', 'apt sync'], correct: 1, explanation: 'La commande "apt update" met a jour l\'index local des paquets disponibles depuis les depots configures dans /etc/apt/sources.list. Elle ne met pas a jour les paquets eux-memes (c\'est "apt upgrade" qui le fait).' },
    { id: 'q100_6', question: 'Quel fichier configure les depots de paquets sur Debian/Ubuntu ?', options: ['/etc/yum.repos.d/', '/etc/apt/sources.list', '/etc/pacman.conf', '/etc/dnf/dnf.conf'], correct: 1, explanation: 'Le fichier /etc/apt/sources.list et le repertoire /etc/apt/sources.list.d/ contiennent la liste des depots APT. Chaque ligne definit un depot avec son type (deb/deb-src), son URL et la distribution ciblee.' },
    { id: 'q100_7', question: 'Quelle distribution est privilegiee pour les environnements de conteneurs minimalistes ?', options: ['Ubuntu Desktop', 'Alpine Linux', 'Kali Linux', 'Linux Mint'], correct: 1, explanation: 'Alpine Linux est une distribution ultra-legere (~5 Mo) basee sur musl libc et BusyBox. Elle est tres populaire comme image de base Docker car elle minimise la taille des conteneurs et la surface d\'attaque.' },
    { id: 'q100_8', question: 'Quelle commande permet de rechercher un paquet disponible dans les depots sur un systeme Debian/Ubuntu ?', options: ['apt find nginx', 'apt search nginx', 'apt locate nginx', 'apt query nginx'], correct: 1, explanation: 'La commande "apt search" recherche dans les noms et descriptions des paquets disponibles dans les depots. On peut aussi utiliser "apt-cache search" pour une recherche plus detaillee.' },
    { id: 'q100_9', question: 'Que signifie LTS dans le contexte des distributions Linux ?', options: ['Linux Terminal Server', 'Long Term Support', 'Latest Tested Software', 'Low Throughput System'], correct: 1, explanation: 'LTS (Long Term Support) designe une version recevant des mises a jour de securite pendant une duree etendue (5 ans pour Ubuntu LTS, 10 ans pour RHEL). Ces versions sont recommandees pour les serveurs de production.' },
    { id: 'q100_10', question: 'Quelle commande supprime un paquet et ses fichiers de configuration sur Debian/Ubuntu ?', options: ['apt remove nginx', 'apt purge nginx', 'apt delete nginx', 'apt uninstall nginx'], correct: 1, explanation: 'La commande "apt purge" supprime le paquet ainsi que ses fichiers de configuration. "apt remove" supprime le paquet mais conserve les fichiers de configuration, ce qui peut etre utile si on prevoit de reinstaller le paquet.' }
  ],
  101: [
    { id: 'q101_1', question: 'Quelle commande affiche le contenu d\'un fichier avec les numeros de ligne ?', options: ['ls -l fichier', 'cat -n fichier', 'head fichier', 'more fichier'], correct: 1, explanation: 'La commande "cat -n" affiche le contenu d\'un fichier avec les numeros de ligne. L\'option -n numerote toutes les lignes, tandis que -b ne numerote que les lignes non vides.' },
    { id: 'q101_2', question: 'Quelle commande recherche recursivement le mot "error" dans tous les fichiers du repertoire courant ?', options: ['find . -name "error"', 'grep -r "error" .', 'locate error', 'search -r "error" .'], correct: 1, explanation: 'La commande "grep -r" (ou --recursive) recherche un motif dans tous les fichiers du repertoire specifie et de ses sous-repertoires. On peut ajouter -i pour ignorer la casse et -n pour afficher les numeros de ligne.' },
    { id: 'q101_3', question: 'Que fait la commande "ls -la" ?', options: ['Liste uniquement les fichiers caches', 'Liste tous les fichiers (y compris caches) avec les details (permissions, taille, date)', 'Liste les fichiers tries par taille', 'Liste les fichiers dans un format compact'], correct: 1, explanation: 'L\'option -l affiche le format long (permissions, proprietaire, taille, date) et -a affiche tous les fichiers, y compris les fichiers caches (commencant par un point). Cette combinaison est l\'une des plus utilisees en administration systeme.' },
    { id: 'q101_4', question: 'Quel operateur redirige la sortie d\'une commande vers un fichier en ecrasant son contenu ?', options: ['|', '>>', '>', '<<'], correct: 2, explanation: 'L\'operateur ">" redirige la sortie standard (stdout) vers un fichier en ecrasant son contenu existant. L\'operateur ">>" ajoute a la fin du fichier sans ecraser. Le pipe "|" transmet la sortie d\'une commande comme entree d\'une autre.' },
    { id: 'q101_5', question: 'Quelle commande sed remplace toutes les occurrences de "http" par "https" dans un fichier ?', options: ['sed "s/http/https/" fichier', 'sed "s/http/https/g" fichier', 'sed "r/http/https/" fichier', 'sed -replace "http" "https" fichier'], correct: 1, explanation: 'La commande sed "s/http/https/g" utilise le flag "g" (global) pour remplacer toutes les occurrences sur chaque ligne. Sans le flag "g", seule la premiere occurrence par ligne est remplacee. L\'option -i permet de modifier le fichier en place.' },
    { id: 'q101_6', question: 'Quelle commande awk affiche la troisieme colonne d\'un fichier ?', options: ['awk \'{print $3}\' fichier', 'awk \'{column 3}\' fichier', 'awk -c 3 fichier', 'awk \'{get $3}\' fichier'], correct: 0, explanation: 'awk utilise $N pour referencer la Nieme colonne (champ). $0 represente la ligne entiere, $1 le premier champ, $2 le deuxieme, etc. Le separateur par defaut est l\'espace ou la tabulation, modifiable avec -F (ex: -F":" pour /etc/passwd).' },
    { id: 'q101_7', question: 'Que fait le pipe (|) dans la commande "cat access.log | grep 404 | wc -l" ?', options: ['Il ecrit la sortie dans un fichier', 'Il transmet la sortie d\'une commande comme entree de la commande suivante', 'Il execute les commandes en parallele', 'Il filtre les erreurs'], correct: 1, explanation: 'Le pipe (|) connecte la sortie standard (stdout) d\'une commande a l\'entree standard (stdin) de la suivante. Ici, cat affiche le fichier, grep filtre les lignes contenant "404", et wc -l compte le nombre de lignes resultantes.' },
    { id: 'q101_8', question: 'Quelle commande affiche les 20 dernieres lignes d\'un fichier et suit les nouvelles lignes ajoutees en temps reel ?', options: ['head -20 -f fichier', 'tail -20 -f fichier', 'cat -f fichier', 'less +F fichier'], correct: 1, explanation: 'La commande "tail -n 20 -f" (ou "tail -20 -f") affiche les 20 dernieres lignes puis continue a afficher les nouvelles lignes ecrites dans le fichier en temps reel. C\'est tres utile pour surveiller les fichiers de log.' },
    { id: 'q101_9', question: 'Quelle redirection envoie a la fois stdout et stderr vers un fichier ?', options: ['commande > fichier', 'commande 2> fichier', 'commande &> fichier', 'commande 1> fichier'], correct: 2, explanation: 'L\'operateur "&>" (ou "2>&1 >") redirige a la fois la sortie standard (fd 1) et la sortie d\'erreur (fd 2) vers un fichier. "2>" redirige uniquement stderr, et ">" redirige uniquement stdout.' },
    { id: 'q101_10', question: 'Quelle commande trie les lignes d\'un fichier et supprime les doublons ?', options: ['sort fichier | unique', 'sort -u fichier', 'uniq fichier', 'sort fichier | distinct'], correct: 1, explanation: 'La commande "sort -u" trie les lignes et supprime les doublons en une seule operation. On peut aussi utiliser "sort fichier | uniq" mais uniq seul ne fonctionne que sur des lignes adjacentes, d\'ou la necessite de trier avant.' }
  ],
  102: [
    { id: 'q102_1', question: 'Quelle commande ajoute un nouvel utilisateur "devops" avec un repertoire personnel ?', options: ['adduser devops', 'useradd -m devops', 'newuser devops', 'user create devops'], correct: 1, explanation: 'La commande "useradd -m" cree un nouvel utilisateur avec l\'option -m qui cree automatiquement le repertoire personnel (/home/devops). Sur Debian/Ubuntu, "adduser" est un script interactif plus convivial qui appelle useradd en arriere-plan.' },
    { id: 'q102_2', question: 'Que signifie la permission "755" sur un fichier ?', options: ['Lecture/ecriture pour tous', 'Lecture/ecriture/execution pour le proprietaire, lecture/execution pour le groupe et les autres', 'Execution pour tous, ecriture pour le proprietaire', 'Aucune permission speciale'], correct: 1, explanation: 'La permission 755 se decompose en : 7 (rwx) pour le proprietaire, 5 (r-x) pour le groupe, et 5 (r-x) pour les autres. C\'est la permission standard pour les repertoires et les scripts executables.' },
    { id: 'q102_3', question: 'Quelle commande change le proprietaire et le groupe d\'un fichier ?', options: ['chmod user:group fichier', 'chown user:group fichier', 'chgrp user:group fichier', 'usermod user:group fichier'], correct: 1, explanation: 'La commande "chown user:group fichier" change a la fois le proprietaire et le groupe d\'un fichier. "chown user fichier" change uniquement le proprietaire. L\'option -R permet de l\'appliquer recursivement a un repertoire.' },
    { id: 'q102_4', question: 'Que fait le bit SUID sur un fichier executable ?', options: ['Il empeche la suppression du fichier', 'Il execute le fichier avec les permissions du proprietaire du fichier, pas de l\'utilisateur qui l\'execute', 'Il force le fichier a etre execute en arriere-plan', 'Il chiffre le fichier a l\'execution'], correct: 1, explanation: 'Le bit SUID (Set User ID, chmod u+s ou 4xxx) fait que le programme s\'execute avec les droits du proprietaire du fichier. Par exemple, /usr/bin/passwd a le SUID pour permettre aux utilisateurs de modifier /etc/shadow (propriete de root).' },
    { id: 'q102_5', question: 'Quelle commande affiche tous les processus en cours avec une vue arborescente ?', options: ['ps aux', 'ps -ef --forest', 'top -tree', 'proc list --tree'], correct: 1, explanation: 'La commande "ps -ef --forest" affiche tous les processus (-e) avec un format complet (-f) et une representation arborescente (--forest) montrant les relations parent-enfant entre les processus. Alternativement, "pstree" offre une vue arborescente dediee.' },
    { id: 'q102_6', question: 'Quelle commande ajoute un utilisateur au groupe "docker" sans modifier ses autres groupes ?', options: ['usermod -g docker utilisateur', 'usermod -aG docker utilisateur', 'groupadd docker utilisateur', 'addgroup utilisateur docker'], correct: 1, explanation: 'La commande "usermod -aG docker utilisateur" utilise -a (append) avec -G (groupe supplementaire) pour ajouter l\'utilisateur au groupe docker sans le retirer de ses autres groupes. Sans -a, l\'utilisateur serait retire de tous ses autres groupes secondaires.' },
    { id: 'q102_7', question: 'Quel fichier contient les informations sur les groupes du systeme ?', options: ['/etc/passwd', '/etc/group', '/etc/shadow', '/etc/users'], correct: 1, explanation: 'Le fichier /etc/group contient la liste des groupes du systeme avec leur GID et leurs membres. /etc/passwd contient les comptes utilisateurs, /etc/shadow les mots de passe hashes, et /etc/sudoers les droits sudo.' },
    { id: 'q102_8', question: 'Que fait le bit SGID sur un repertoire ?', options: ['Il empeche les autres utilisateurs d\'acceder au repertoire', 'Les nouveaux fichiers crees dans ce repertoire heritent du groupe du repertoire plutot que du groupe primaire du createur', 'Il rend le repertoire en lecture seule', 'Il supprime automatiquement les fichiers apres 30 jours'], correct: 1, explanation: 'Le bit SGID (Set Group ID, chmod g+s ou 2xxx) sur un repertoire fait que les nouveaux fichiers crees a l\'interieur heritent du groupe du repertoire. C\'est utile pour les repertoires partages ou plusieurs utilisateurs doivent collaborer.' },
    { id: 'q102_9', question: 'Quelle commande envoie le signal SIGKILL au processus ayant le PID 1234 ?', options: ['kill 1234', 'kill -9 1234', 'stop 1234', 'terminate -force 1234'], correct: 1, explanation: 'La commande "kill -9" envoie le signal SIGKILL (9) qui termine immediatement le processus sans lui laisser le temps de faire du nettoyage. "kill" sans option envoie SIGTERM (15) qui permet au processus de terminer proprement. SIGKILL ne peut pas etre intercepte.' },
    { id: 'q102_10', question: 'Quelle commande chmod ajoute le droit d\'execution pour le proprietaire sur un script ?', options: ['chmod +x script.sh', 'chmod u+x script.sh', 'chmod 700 script.sh', 'chmod a+x script.sh'], correct: 1, explanation: 'La commande "chmod u+x" ajoute le droit d\'execution (x) uniquement pour le proprietaire (u = user). "chmod +x" ou "chmod a+x" ajoute l\'execution pour tous (proprietaire, groupe, autres). "chmod 700" donne rwx uniquement au proprietaire.' }
  ],
  103: [
    { id: 'q103_1', question: 'Quel est le role de systemd dans un systeme Linux moderne ?', options: ['C\'est un editeur de texte en ligne de commande', 'C\'est le systeme d\'init (PID 1) qui gere le demarrage du systeme, les services, les sockets et les timers', 'C\'est un gestionnaire de paquets', 'C\'est un outil de monitoring reseau'], correct: 1, explanation: 'systemd est le systeme d\'init par defaut de la plupart des distributions Linux modernes. Il est le premier processus demarre (PID 1) et gere les services (daemons), les points de montage, les sockets, les timers et les cgroups.' },
    { id: 'q103_2', question: 'Quelle commande demarre le service nginx et l\'active au demarrage du systeme ?', options: ['systemctl start nginx && systemctl enable nginx', 'service nginx start --enable', 'init.d start nginx --boot', 'systemd start nginx --persist'], correct: 0, explanation: '"systemctl start nginx" demarre le service immediatement, et "systemctl enable nginx" cree un lien symbolique pour le demarrage automatique au boot. On peut combiner les deux avec "systemctl enable --now nginx".' },
    { id: 'q103_3', question: 'Ou se trouvent les fichiers unit systemd fournis par les paquets installes ?', options: ['/etc/systemd/system/', '/usr/lib/systemd/system/', '/var/systemd/units/', '/opt/systemd/services/'], correct: 1, explanation: 'Les fichiers unit fournis par les paquets sont dans /usr/lib/systemd/system/. Les personnalisations de l\'administrateur vont dans /etc/systemd/system/ qui a la priorite. On utilise aussi "systemctl edit" pour creer des overrides dans /etc/systemd/system/service.d/.' },
    { id: 'q103_4', question: 'Quelle section d\'un fichier unit systemd definit les dependances et l\'ordre de demarrage ?', options: ['[Service]', '[Unit]', '[Install]', '[Dependencies]'], correct: 1, explanation: 'La section [Unit] contient les metadonnees du service et ses dependances : Description, After (ordre de demarrage), Requires (dependances obligatoires), Wants (dependances optionnelles) et Conflicts (services incompatibles).' },
    { id: 'q103_5', question: 'Quelle directive dans un fichier unit specifie que le service doit redemarrer automatiquement en cas de crash ?', options: ['AutoRestart=true', 'Restart=on-failure', 'Respawn=yes', 'Recovery=auto'], correct: 1, explanation: 'La directive "Restart=on-failure" dans la section [Service] fait que systemd redemarre automatiquement le service s\'il se termine avec un code d\'erreur. Autres valeurs possibles : "always" (redemarre toujours), "on-abnormal", "on-abort", "no" (defaut).' },
    { id: 'q103_6', question: 'Quelle commande affiche les logs du service nginx depuis le dernier demarrage ?', options: ['systemctl logs nginx', 'journalctl -u nginx -b', 'cat /var/log/nginx/systemd.log', 'syslog -service nginx'], correct: 1, explanation: 'La commande "journalctl -u nginx -b" utilise -u pour filtrer par unite (service nginx) et -b pour limiter au boot actuel. journalctl est l\'outil de consultation du journal systemd (journald) qui centralise tous les logs.' },
    { id: 'q103_7', question: 'Apres avoir modifie un fichier unit systemd, quelle commande doit-on executer ?', options: ['systemctl restart-all', 'systemctl daemon-reload', 'systemd refresh', 'service reload-config'], correct: 1, explanation: '"systemctl daemon-reload" recharge la configuration de systemd pour prendre en compte les modifications des fichiers unit. Sans cette commande, systemd utilise l\'ancienne version en memoire. Il faut ensuite redemarrer le service concerne.' },
    { id: 'q103_8', question: 'Quelle commande journalctl affiche les logs en temps reel (comme tail -f) ?', options: ['journalctl --live', 'journalctl -f', 'journalctl --stream', 'journalctl -w'], correct: 1, explanation: 'L\'option -f (--follow) de journalctl affiche les derniers logs puis continue d\'afficher les nouvelles entrees en temps reel, similaire a "tail -f". On peut combiner avec -u pour suivre un service specifique : "journalctl -fu nginx".' },
    { id: 'q103_9', question: 'Quelle directive definit le type de demarrage d\'un service systemd ?', options: ['StartType=daemon', 'Type=notify', 'Mode=forking', 'ExecType=simple'], correct: 1, explanation: 'La directive "Type=" dans la section [Service] definit comment systemd detecte que le service est pret. Valeurs : "simple" (defaut, le processus principal est le service), "forking" (le processus fork en arriere-plan), "notify" (le service notifie systemd via sd_notify), "oneshot" (execute une tache puis se termine).' },
    { id: 'q103_10', question: 'Quelle commande liste toutes les unites systemd en echec ?', options: ['systemctl list-units --failed', 'systemctl status --errors', 'systemd check-failed', 'journalctl --failed-units'], correct: 0, explanation: '"systemctl list-units --failed" (ou "systemctl --failed") affiche toutes les unites qui sont dans un etat d\'echec. C\'est une des premieres commandes a executer lors du diagnostic de problemes systeme.' }
  ],
  104: [
    { id: 'q104_1', question: 'Quel repertoire contient la majorite des fichiers de log sur un systeme Linux ?', options: ['/etc/log/', '/var/log/', '/tmp/logs/', '/usr/log/'], correct: 1, explanation: 'Le repertoire /var/log/ est le standard FHS (Filesystem Hierarchy Standard) pour les fichiers de log. On y trouve syslog, auth.log, kern.log, dmesg, les logs applicatifs (nginx, apache, mysql), et les logs systemd (journal).' },
    { id: 'q104_2', question: 'Quel est le role de rsyslog sur un systeme Linux ?', options: ['C\'est un outil de monitoring CPU', 'C\'est un daemon de journalisation qui collecte, filtre et transmet les messages de log systeme', 'C\'est un gestionnaire de fichiers temporaires', 'C\'est un outil de backup automatique'], correct: 1, explanation: 'rsyslog (rocket-fast system for log processing) est un daemon de journalisation systeme qui recoit les messages de log via syslog, les filtre selon des regles configurables, et les ecrit dans des fichiers ou les transmet a des serveurs distants. Il est configure dans /etc/rsyslog.conf.' },
    { id: 'q104_3', question: 'Quelle est la difference entre journald (systemd-journald) et rsyslog ?', options: ['journald est plus ancien que rsyslog', 'journald stocke les logs en format binaire structure et est integre a systemd, tandis que rsyslog utilise des fichiers texte', 'rsyslog est integre a systemd et journald est independant', 'Il n\'y a aucune difference, ce sont des alias'], correct: 1, explanation: 'journald fait partie de systemd et stocke les logs dans un format binaire indexe (/var/log/journal/), consultable avec journalctl. rsyslog utilise des fichiers texte traditionnels dans /var/log/. Les deux peuvent coexister, journald transmettant les logs a rsyslog.' },
    { id: 'q104_4', question: 'Quel fichier de log enregistre les tentatives de connexion et les evenements d\'authentification sur Debian/Ubuntu ?', options: ['/var/log/syslog', '/var/log/auth.log', '/var/log/messages', '/var/log/login.log'], correct: 1, explanation: 'Le fichier /var/log/auth.log (ou /var/log/secure sur RHEL/CentOS) enregistre tous les evenements d\'authentification : connexions SSH, sudo, su, echecs de connexion, et les changements de mot de passe. C\'est essentiel pour la securite.' },
    { id: 'q104_5', question: 'Quel outil gere la rotation automatique des fichiers de log pour eviter qu\'ils ne remplissent le disque ?', options: ['logmanager', 'logrotate', 'logcleaner', 'syslog-rotate'], correct: 1, explanation: 'logrotate est l\'outil standard de rotation des logs sous Linux. Configure dans /etc/logrotate.conf et /etc/logrotate.d/, il compresse, renomme et supprime les anciens fichiers de log selon des regles (quotidien, hebdomadaire, nombre de rotations, taille maximale).' },
    { id: 'q104_6', question: 'Quelle directive logrotate definit le nombre de fichiers de log archives a conserver ?', options: ['keep 7', 'rotate 7', 'maxfiles 7', 'retention 7'], correct: 1, explanation: 'La directive "rotate 7" dans la configuration logrotate indique de conserver 7 fichiers d\'archive. Les fichiers plus anciens sont automatiquement supprimes. On peut aussi definir la frequence (daily, weekly, monthly) et la compression (compress).' },
    { id: 'q104_7', question: 'Quelle commande journalctl filtre les logs par niveau de priorite "error" et superieur ?', options: ['journalctl --level=error', 'journalctl -p err', 'journalctl --severity error', 'journalctl -filter error'], correct: 1, explanation: 'L\'option -p (priority) de journalctl filtre par niveau syslog : emerg (0), alert (1), crit (2), err (3), warning (4), notice (5), info (6), debug (7). "-p err" affiche les niveaux err, crit, alert et emerg.' },
    { id: 'q104_8', question: 'Quel fichier de log contient les messages du noyau Linux ?', options: ['/var/log/boot.log', '/var/log/kern.log', '/var/log/system.log', '/var/log/kernel.log'], correct: 1, explanation: 'Le fichier /var/log/kern.log contient les messages du noyau Linux (kernel). On peut aussi consulter les messages du noyau avec la commande "dmesg" qui lit le tampon de messages du noyau en memoire.' },
    { id: 'q104_9', question: 'Quelle commande affiche les logs journald entre deux dates specifiques ?', options: ['journalctl --range "2024-01-01" "2024-01-31"', 'journalctl --since "2024-01-01" --until "2024-01-31"', 'journalctl -d "2024-01-01..2024-01-31"', 'journalctl --from "2024-01-01" --to "2024-01-31"'], correct: 1, explanation: 'Les options --since et --until de journalctl permettent de filtrer les logs par plage temporelle. Le format accepte "YYYY-MM-DD HH:MM:SS", mais aussi des expressions comme "yesterday", "1 hour ago" ou "today".' },
    { id: 'q104_10', question: 'Dans rsyslog, que signifie la regle "*.crit /var/log/critical.log" ?', options: ['Tous les messages de tous les niveaux sont ecrits dans critical.log', 'Tous les messages de priorite critique et superieure de toutes les facilities sont ecrits dans critical.log', 'Seuls les messages de priorite critique sont ecrits, les plus eleves sont exclus', 'Le fichier critical.log est cree uniquement en cas d\'urgence'], correct: 1, explanation: 'Dans la syntaxe rsyslog, "*" represente toutes les facilities (kern, auth, mail, etc.) et ".crit" indique le niveau critique et au-dessus. La regle dirige donc les messages crit, alert et emerg de toutes les sources vers /var/log/critical.log.' }
  ],
  105: [
    { id: 'q105_1', question: 'Comment declare-t-on une variable en Bash et comment l\'utilise-t-on ?', options: ['var NAME = "valeur" puis echo NAME', 'NAME="valeur" puis echo $NAME', 'set NAME "valeur" puis echo %NAME%', 'declare NAME := "valeur" puis echo ${NAME}'], correct: 1, explanation: 'En Bash, on declare une variable sans espaces autour du "=" : NAME="valeur". On y accede avec le prefixe $ : echo $NAME ou echo ${NAME}. Les accolades sont utiles pour delimiter le nom : ${NAME}_suffix.' },
    { id: 'q105_2', question: 'Quelle est la syntaxe correcte d\'une boucle for en Bash pour iterer sur une liste ?', options: ['for (item in liste) { echo $item; }', 'for item in liste; do echo "$item"; done', 'foreach item (liste) echo $item end', 'for item = liste; echo $item; next'], correct: 1, explanation: 'La syntaxe Bash est "for variable in liste; do commandes; done". La liste peut etre des mots separes par des espaces, une expansion de glob (*.txt), ou une sequence ($(seq 1 10) ou {1..10}).' },
    { id: 'q105_3', question: 'Quelle variable speciale Bash contient le code de retour de la derniere commande executee ?', options: ['$0', '$#', '$?', '$!'], correct: 2, explanation: 'La variable $? contient le code de retour (exit code) de la derniere commande : 0 signifie succes, toute autre valeur indique une erreur. $0 est le nom du script, $# le nombre d\'arguments, et $! le PID du dernier processus en arriere-plan.' },
    { id: 'q105_4', question: 'Quelle syntaxe definit une fonction en Bash ?', options: ['function backup { ... }', 'def backup(): ...', 'func backup() { ... }', 'sub backup { ... }'], correct: 0, explanation: 'En Bash, on definit une fonction avec "function nom { ... }" ou "nom() { ... }". Les arguments sont accessibles via $1, $2, etc. Les fonctions n\'ont pas de declaration de parametres explicite. Le retour se fait avec "return N" (code de retour) ou echo (sortie).' },
    { id: 'q105_5', question: 'Quelle est la syntaxe correcte d\'un test conditionnel en Bash pour verifier si un fichier existe ?', options: ['if exist "/tmp/file"; then', 'if [ -f "/tmp/file" ]; then', 'if test("/tmp/file"); then', 'if file.exists("/tmp/file"); then'], correct: 1, explanation: 'La syntaxe "[ -f fichier ]" teste l\'existence d\'un fichier regulier. Les crochets [ ] sont un alias pour la commande "test". -d teste un repertoire, -e teste l\'existence (fichier ou repertoire), -r/-w/-x testent les permissions de lecture/ecriture/execution.' },
    { id: 'q105_6', question: 'Quelle ligne doit figurer en premiere ligne d\'un script Bash ?', options: ['#!/bin/sh', '#!/bin/bash', '#!bash', '// bash script'], correct: 1, explanation: 'Le shebang "#!/bin/bash" indique au systeme d\'utiliser l\'interpreteur Bash pour executer le script. #!/bin/sh utilise le shell POSIX (qui peut ne pas etre Bash). Sans shebang, le script est execute par le shell courant de l\'utilisateur.' },
    { id: 'q105_7', question: 'Quelle expression cron planifie une tache tous les jours a 3h du matin ?', options: ['0 3 * * *', '3 0 * * *', '* 3 * * *', '0 0 3 * *'], correct: 0, explanation: 'Le format cron est "minute heure jour_du_mois mois jour_de_la_semaine". "0 3 * * *" signifie minute 0, heure 3, tous les jours, tous les mois, tous les jours de la semaine. "* 3 * * *" s\'executerait chaque minute pendant l\'heure 3 (60 fois).' },
    { id: 'q105_8', question: 'Quelle commande Bash itere sur les lignes d\'un fichier ?', options: ['for line in $(cat fichier); do', 'while IFS= read -r line; do ... done < fichier', 'foreach line in fichier; do', 'read fichier | for line; do'], correct: 1, explanation: '"while IFS= read -r line; do ... done < fichier" est la methode correcte pour lire un fichier ligne par ligne. IFS= empeche le trim des espaces, -r empeche l\'interpretation des backslashs. "for line in $(cat fichier)" decoupe par mots, pas par lignes.' },
    { id: 'q105_9', question: 'Comment rendre un script Bash executable ?', options: ['bash --enable script.sh', 'chmod +x script.sh', 'exec script.sh', 'enable script.sh'], correct: 1, explanation: 'La commande "chmod +x script.sh" ajoute le droit d\'execution au fichier. On peut ensuite l\'executer avec "./script.sh" (si le shebang est present) ou "bash script.sh" (execution explicite par l\'interpreteur Bash).' },
    { id: 'q105_10', question: 'Quelle commande crontab affiche les taches cron de l\'utilisateur courant ?', options: ['crontab -e', 'crontab -l', 'cron list', 'crontab --show'], correct: 1, explanation: '"crontab -l" liste les taches cron de l\'utilisateur courant. "crontab -e" ouvre l\'editeur pour modifier les taches. "crontab -r" supprime toutes les taches (attention, pas de confirmation). Les taches systeme sont dans /etc/crontab et /etc/cron.d/.' }
  ]
}
