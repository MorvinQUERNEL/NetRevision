import type { QuizQuestion } from '../quizzes'

export const quizzes: Record<number, QuizQuestion[]> = {
  106: [
    { id: 'q106_1', question: 'Quelle commande affiche la configuration IP de toutes les interfaces reseau sur Linux ?', options: ['ifconfig -a', 'ip addr show', 'netstat -i', 'route -n'], correct: 1, explanation: 'La commande "ip addr show" (ou "ip a") est l\'outil moderne pour afficher les adresses IP de toutes les interfaces. ifconfig est obsolete sur les distributions modernes et fait partie du paquet net-tools.' },
    { id: 'q106_2', question: 'Quel fichier configure les interfaces reseau sur Ubuntu 18.04+ avec Netplan ?', options: ['/etc/network/interfaces', '/etc/netplan/*.yaml', '/etc/sysconfig/network-scripts/', '/etc/NetworkManager/'], correct: 1, explanation: 'Netplan utilise des fichiers YAML dans /etc/netplan/ pour configurer le reseau sur Ubuntu 18.04+. Il genere ensuite la configuration pour le backend (systemd-networkd ou NetworkManager). La commande "netplan apply" applique les changements.' },
    { id: 'q106_3', question: 'Quelle commande teste la connectivite reseau vers un hote et affiche le temps de reponse ?', options: ['traceroute hote', 'ping hote', 'nslookup hote', 'dig hote'], correct: 1, explanation: 'La commande "ping" envoie des paquets ICMP Echo Request a l\'hote cible et affiche le temps aller-retour (RTT). C\'est l\'outil de base pour verifier la connectivite reseau. L\'option -c limite le nombre de paquets envoyes.' },
    { id: 'q106_4', question: 'Quelle commande affiche les ports en ecoute et les processus associes sur Linux ?', options: ['ps aux | grep port', 'ss -tlnp', 'netstat -r', 'lsof -i :80'], correct: 1, explanation: 'La commande "ss -tlnp" affiche les sockets TCP (-t) en ecoute (-l), avec les numeros de port (-n) et les processus (-p). ss (socket statistics) est le remplacant moderne de netstat et est plus rapide.' },
    { id: 'q106_5', question: 'Quel protocole de couche 4 est utilise par SSH pour garantir une connexion fiable ?', options: ['UDP', 'TCP', 'ICMP', 'SCTP'], correct: 1, explanation: 'SSH utilise TCP (port 22 par defaut) car il necessite une connexion fiable, ordonnee et sans perte de donnees. TCP fournit le controle de flux, la retransmission des paquets perdus et la livraison dans l\'ordre.' },
    { id: 'q106_6', question: 'Quelle commande affiche la table de routage du systeme sur Linux ?', options: ['route display', 'ip route show', 'routing table', 'netstat -routes'], correct: 1, explanation: 'La commande "ip route show" (ou "ip r") affiche la table de routage du noyau Linux. Elle montre les reseaux de destination, les passerelles, les interfaces et les metriques. "ip route add" permet d\'ajouter des routes.' },
    { id: 'q106_7', question: 'Sur quel port par defaut fonctionne le protocole HTTPS ?', options: ['80', '443', '8080', '8443'], correct: 1, explanation: 'HTTPS (HTTP over TLS) utilise le port TCP 443 par defaut. HTTP utilise le port 80, 8080 est souvent utilise pour les proxys ou serveurs de developpement, et 8443 est un port alternatif courant pour HTTPS.' },
    { id: 'q106_8', question: 'Quelle commande curl envoie une requete POST avec un body JSON ?', options: ['curl -X POST -d \'{"key":"value"}\' URL', 'curl -X POST -H "Content-Type: application/json" -d \'{"key":"value"}\' URL', 'curl --post --json \'{"key":"value"}\' URL', 'curl -method POST -body \'{"key":"value"}\' URL'], correct: 1, explanation: 'Pour envoyer du JSON avec curl, il faut specifier le header Content-Type avec -H et le body avec -d. Sans le header, le serveur peut interpreter le body comme du form-data. L\'option -X POST est implicite quand -d est utilise.' },
    { id: 'q106_9', question: 'Quelle commande affiche le chemin emprunte par les paquets vers un hote distant ?', options: ['ping -t hote', 'traceroute hote', 'route hote', 'pathping hote'], correct: 1, explanation: 'traceroute (ou tracepath) affiche chaque saut (routeur) entre la source et la destination en envoyant des paquets avec des TTL incrementaux. C\'est essentiel pour diagnostiquer les problemes de routage et identifier les goulots d\'etranglement.' },
    { id: 'q106_10', question: 'Quel outil permet de capturer et analyser le trafic reseau en ligne de commande ?', options: ['netstat', 'tcpdump', 'nmap', 'iptables'], correct: 1, explanation: 'tcpdump est un outil de capture de paquets en ligne de commande. Il permet de filtrer et analyser le trafic reseau en temps reel ou de l\'enregistrer dans un fichier .pcap pour analyse ulterieure avec Wireshark.' }
  ],
  107: [
    { id: 'q107_1', question: 'Quel type d\'enregistrement DNS associe un nom de domaine a une adresse IPv4 ?', options: ['AAAA', 'CNAME', 'A', 'MX'], correct: 2, explanation: 'L\'enregistrement A (Address) mappe un nom de domaine a une adresse IPv4. AAAA est pour IPv6, CNAME cree un alias vers un autre nom, et MX definit les serveurs de messagerie du domaine.' },
    { id: 'q107_2', question: 'Quelle commande permet de tester la resolution DNS d\'un domaine ?', options: ['ping domaine', 'dig domaine', 'traceroute domaine', 'curl domaine'], correct: 1, explanation: 'La commande "dig" (Domain Information Groper) interroge les serveurs DNS et affiche la reponse complete incluant les sections Question, Answer, Authority et Additional. On peut aussi utiliser "nslookup" ou "host" pour des requetes plus simples.' },
    { id: 'q107_3', question: 'Quel fichier local permet de definir des resolutions DNS statiques sur Linux ?', options: ['/etc/resolv.conf', '/etc/hosts', '/etc/dns.conf', '/etc/hostname'], correct: 1, explanation: 'Le fichier /etc/hosts contient des associations statiques nom-IP qui sont consultees avant les serveurs DNS (selon la configuration de /etc/nsswitch.conf). C\'est utile pour le developpement local ou pour bloquer des domaines.' },
    { id: 'q107_4', question: 'Quel enregistrement DNS est utilise pour la delegation de sous-domaine ?', options: ['A', 'NS', 'SOA', 'PTR'], correct: 1, explanation: 'L\'enregistrement NS (Name Server) indique quels serveurs DNS font autorite pour une zone ou un sous-domaine. La delegation consiste a creer des enregistrements NS pointant vers les serveurs DNS du sous-domaine delegue.' },
    { id: 'q107_5', question: 'Que signifie le TTL (Time To Live) dans un enregistrement DNS ?', options: ['Le temps de vie du serveur DNS', 'La duree pendant laquelle un enregistrement DNS peut etre mis en cache par les resolvers', 'Le temps maximum de reponse du serveur DNS', 'Le nombre de sauts reseau avant expiration'], correct: 1, explanation: 'Le TTL DNS indique combien de temps (en secondes) un resolver peut garder l\'enregistrement en cache avant de le re-interroger. Un TTL court (300s) permet des changements rapides, un TTL long (86400s) reduit la charge DNS.' },
    { id: 'q107_6', question: 'Quel serveur DNS open-source est le plus utilise sur Linux pour heberger des zones DNS ?', options: ['Apache', 'BIND (named)', 'Postfix', 'Squid'], correct: 1, explanation: 'BIND (Berkeley Internet Name Domain) est le serveur DNS le plus deploye au monde. Le daemon "named" gere les zones DNS. D\'autres alternatives existent comme PowerDNS, Unbound (resolver) et CoreDNS (utilise par Kubernetes).' },
    { id: 'q107_7', question: 'Quel fichier configure les serveurs DNS utilises par le systeme sur Linux ?', options: ['/etc/hosts', '/etc/resolv.conf', '/etc/dns/servers', '/etc/nameservers'], correct: 1, explanation: 'Le fichier /etc/resolv.conf contient les serveurs DNS (nameserver) et les options de resolution. Attention : sur les systemes utilisant systemd-resolved ou NetworkManager, ce fichier peut etre genere automatiquement.' },
    { id: 'q107_8', question: 'Quel type d\'enregistrement DNS permet de creer un alias vers un autre nom de domaine ?', options: ['A', 'MX', 'CNAME', 'TXT'], correct: 2, explanation: 'L\'enregistrement CNAME (Canonical Name) cree un alias pointant vers un autre nom de domaine. Par exemple, www.example.com CNAME example.com. Attention : un CNAME ne peut pas coexister avec d\'autres enregistrements pour le meme nom.' },
    { id: 'q107_9', question: 'Quelle commande dig interroge un serveur DNS specifique pour un enregistrement MX ?', options: ['dig MX example.com', 'dig @8.8.8.8 example.com MX', 'dig -server 8.8.8.8 -type MX example.com', 'dig example.com -query MX -dns 8.8.8.8'], correct: 1, explanation: 'La syntaxe "dig @serveur domaine type" interroge un serveur DNS specifique. "dig @8.8.8.8 example.com MX" interroge le DNS Google (8.8.8.8) pour les enregistrements MX du domaine. Sans @, dig utilise les serveurs de /etc/resolv.conf.' },
    { id: 'q107_10', question: 'Quel enregistrement DNS est utilise pour la verification de la propriete d\'un domaine et la configuration SPF ?', options: ['A', 'NS', 'TXT', 'SRV'], correct: 2, explanation: 'L\'enregistrement TXT permet de stocker du texte arbitraire. Il est utilise pour SPF (Sender Policy Framework), DKIM, DMARC (securite email), la verification de propriete (Google, Let\'s Encrypt) et d\'autres mecanismes de validation.' }
  ],
  108: [
    { id: 'q108_1', question: 'Quel outil de firewall est le plus couramment utilise sur les distributions modernes avec systemd ?', options: ['iptables', 'firewalld', 'ufw', 'pf'], correct: 1, explanation: 'firewalld est le firewall par defaut sur les distributions RHEL/CentOS/Fedora avec systemd. Il utilise des zones (public, internal, etc.) et gere dynamiquement les regles sans redemarrage. Sur Ubuntu, ufw (Uncomplicated Firewall) est le frontend simplifie d\'iptables.' },
    { id: 'q108_2', question: 'Quelle commande ufw autorise le trafic SSH entrant ?', options: ['ufw add ssh', 'ufw allow ssh', 'ufw open 22', 'ufw permit ssh'], correct: 1, explanation: 'La commande "ufw allow ssh" (ou "ufw allow 22/tcp") autorise le trafic entrant sur le port 22. ufw est un frontend simplifie pour iptables sur Ubuntu. Il faut d\'abord l\'activer avec "ufw enable" et verifier avec "ufw status".' },
    { id: 'q108_3', question: 'Dans iptables, quelle chaine traite les paquets destines au systeme local ?', options: ['OUTPUT', 'FORWARD', 'INPUT', 'PREROUTING'], correct: 2, explanation: 'La chaine INPUT traite les paquets entrants destines au systeme local. OUTPUT traite les paquets generes localement, et FORWARD traite les paquets en transit (routage). PREROUTING et POSTROUTING sont dans la table nat.' },
    { id: 'q108_4', question: 'Quelle commande iptables bloque tout le trafic entrant sauf SSH ?', options: ['iptables -A INPUT -p tcp --dport 22 -j ACCEPT && iptables -P INPUT DROP', 'iptables -P INPUT DROP && iptables -A INPUT -p tcp --dport 22 -j ACCEPT', 'iptables --block-all --except ssh', 'iptables -D INPUT -p all -j REJECT'], correct: 0, explanation: 'On ajoute d\'abord la regle autorisant SSH (-A INPUT -p tcp --dport 22 -j ACCEPT) puis on definit la politique par defaut a DROP (-P INPUT DROP). L\'ordre est important : si on met DROP d\'abord en SSH distant, on perd la connexion.' },
    { id: 'q108_5', question: 'Quel est le role de nftables par rapport a iptables ?', options: ['nftables est un outil de monitoring reseau', 'nftables est le successeur d\'iptables avec une syntaxe unifiee et de meilleures performances', 'nftables est specifique aux systemes BSD', 'nftables ne gere que le trafic IPv6'], correct: 1, explanation: 'nftables remplace iptables, ip6tables, arptables et ebtables avec un framework unifie. Il offre une syntaxe plus lisible, de meilleures performances grace a une architecture basee sur des maps et sets, et une API Netlink native.' },
    { id: 'q108_6', question: 'Quelle commande firewalld ajoute le port 8080/tcp a la zone publique de facon permanente ?', options: ['firewall-cmd --zone=public --add-port=8080/tcp --permanent', 'firewalld --open 8080 --zone=public --save', 'firewall-cmd --allow 8080/tcp --persist', 'firewalld-cmd --port 8080 --add --permanent'], correct: 0, explanation: 'La commande utilise --zone pour specifier la zone, --add-port pour ouvrir un port, et --permanent pour persister la regle apres redemarrage. Sans --permanent, la regle est temporaire. Il faut ensuite "firewall-cmd --reload" pour appliquer.' },
    { id: 'q108_7', question: 'Quel concept de firewalld permet de regrouper des regles par niveau de confiance ?', options: ['Les tables', 'Les zones', 'Les chaines', 'Les profils'], correct: 1, explanation: 'firewalld utilise des zones (public, trusted, internal, dmz, etc.) pour definir le niveau de confiance d\'un reseau. Chaque interface reseau est assignee a une zone, et chaque zone a ses propres regles. La zone par defaut est generalement "public".' },
    { id: 'q108_8', question: 'Quelle commande iptables affiche toutes les regles avec les compteurs de paquets ?', options: ['iptables -L', 'iptables -L -v -n', 'iptables --list-all', 'iptables -show'], correct: 1, explanation: 'L\'option -L liste les regles, -v (verbose) ajoute les compteurs de paquets et d\'octets, et -n affiche les adresses IP et ports en numerique (sans resolution DNS). C\'est la commande standard pour diagnostiquer les regles de firewall.' },
    { id: 'q108_9', question: 'Que fait la cible REJECT dans iptables par rapport a DROP ?', options: ['REJECT est identique a DROP', 'REJECT renvoie un message d\'erreur ICMP a l\'expediteur, DROP ignore silencieusement le paquet', 'DROP renvoie un message d\'erreur, REJECT ignore le paquet', 'REJECT enregistre le paquet dans les logs, DROP non'], correct: 1, explanation: 'DROP ignore silencieusement le paquet (l\'expediteur ne recoit rien et doit attendre le timeout). REJECT envoie un message ICMP "port unreachable" ou TCP RST. DROP est plus securise (stealth) mais REJECT est plus poli et evite les timeouts cote client.' },
    { id: 'q108_10', question: 'Quelle commande ufw affiche les regles numerotees pour faciliter la suppression ?', options: ['ufw list', 'ufw status numbered', 'ufw rules --numbered', 'ufw show numbers'], correct: 1, explanation: '"ufw status numbered" affiche les regles avec un numero d\'ordre. On peut ensuite supprimer une regle specifique avec "ufw delete N" ou le numero est celui affiche. C\'est plus pratique que de retaper la regle complete.' }
  ],
  109: [
    { id: 'q109_1', question: 'Quel est le role d\'un reverse proxy dans une architecture web ?', options: ['Il cache l\'adresse IP du client au serveur', 'Il recoit les requetes des clients et les transmet aux serveurs backend, masquant l\'infrastructure interne', 'Il accelere uniquement les telechargements de fichiers', 'Il remplace le serveur DNS'], correct: 1, explanation: 'Un reverse proxy se place devant les serveurs backend et recoit toutes les requetes des clients. Il offre du load balancing, du caching, la terminaison SSL, la compression, et masque l\'architecture interne. Nginx et HAProxy sont les plus populaires.' },
    { id: 'q109_2', question: 'Quelle directive Nginx configure un reverse proxy vers un serveur backend ?', options: ['redirect_to http://backend:3000', 'proxy_pass http://backend:3000', 'forward http://backend:3000', 'backend_server http://backend:3000'], correct: 1, explanation: 'La directive "proxy_pass" dans un bloc location transmet les requetes au serveur backend specifie. On ajoute generalement proxy_set_header pour transmettre les headers originaux (Host, X-Real-IP, X-Forwarded-For, X-Forwarded-Proto).' },
    { id: 'q109_3', question: 'Quel algorithme de load balancing distribue les requetes de maniere egale entre les serveurs ?', options: ['ip_hash', 'round-robin', 'least_conn', 'random'], correct: 1, explanation: 'Le round-robin distribue les requetes de maniere sequentielle et equitable entre les serveurs du pool. C\'est l\'algorithme par defaut de Nginx. ip_hash assure la persistance de session, et least_conn envoie au serveur ayant le moins de connexions actives.' },
    { id: 'q109_4', question: 'Quelle directive Nginx definit un groupe de serveurs backend pour le load balancing ?', options: ['server_group', 'upstream', 'backend_pool', 'load_balance'], correct: 1, explanation: 'Le bloc "upstream" definit un groupe de serveurs backend. Exemple : upstream backend { server 10.0.0.1:3000; server 10.0.0.2:3000; }. On reference ensuite ce groupe avec proxy_pass http://backend.' },
    { id: 'q109_5', question: 'Quelle commande verifie la syntaxe de la configuration Nginx avant de recharger ?', options: ['nginx -check', 'nginx -t', 'nginx --validate', 'nginx -syntax'], correct: 1, explanation: '"nginx -t" (test) verifie la syntaxe de tous les fichiers de configuration sans redemarrer le service. C\'est une bonne pratique de toujours tester avant de recharger. "nginx -T" teste et affiche la configuration complete.' },
    { id: 'q109_6', question: 'Que fait la commande "systemctl reload nginx" par rapport a "systemctl restart nginx" ?', options: ['Elles sont identiques', 'reload recharge la configuration sans couper les connexions existantes, restart arrete et redemarre le processus', 'restart est plus rapide que reload', 'reload ne fonctionne que pour les fichiers statiques'], correct: 1, explanation: '"reload" envoie un signal SIGHUP a Nginx qui recharge la configuration de maniere gracieuse (les connexions en cours sont terminees normalement). "restart" arrete le processus et en demarre un nouveau, coupant toutes les connexions actives.' },
    { id: 'q109_7', question: 'Quel header HTTP permet au reverse proxy de transmettre l\'adresse IP reelle du client au backend ?', options: ['X-Client-IP', 'X-Forwarded-For', 'X-Origin-IP', 'Client-Address'], correct: 1, explanation: 'Le header X-Forwarded-For (XFF) contient l\'adresse IP du client original. Le reverse proxy l\'ajoute avec "proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for". Sans ce header, le backend ne voit que l\'IP du proxy.' },
    { id: 'q109_8', question: 'Quelle directive Nginx active le caching des reponses du backend ?', options: ['cache_enable on', 'proxy_cache zone_name', 'enable_caching true', 'backend_cache on'], correct: 1, explanation: 'Le caching Nginx necessite deux elements : "proxy_cache_path" (dans le bloc http) pour definir le chemin et les parametres du cache, et "proxy_cache zone_name" (dans le bloc location) pour activer le cache sur les requetes specifiques.' },
    { id: 'q109_9', question: 'Quel outil en ligne de commande est souvent utilise comme reverse proxy TCP/HTTP haute performance ?', options: ['Apache', 'HAProxy', 'Squid', 'Varnish'], correct: 1, explanation: 'HAProxy (High Availability Proxy) est un reverse proxy et load balancer open-source tres performant. Il gere le TCP et l\'HTTP, supporte les health checks, la persistance de session et les ACL. Il est souvent utilise devant des clusters de serveurs.' },
    { id: 'q109_10', question: 'Quelle est la difference entre un forward proxy et un reverse proxy ?', options: ['Il n\'y a pas de difference', 'Un forward proxy agit pour le compte des clients (sortant), un reverse proxy agit pour le compte des serveurs (entrant)', 'Un forward proxy est plus rapide', 'Un reverse proxy ne supporte pas HTTPS'], correct: 1, explanation: 'Un forward proxy (ex: Squid) est utilise par les clients pour acceder a Internet (anonymisation, filtrage, cache). Un reverse proxy (ex: Nginx) est place devant les serveurs pour recevoir les requetes entrantes (load balancing, SSL, cache).' }
  ],
  110: [
    { id: 'q110_1', question: 'Quelle commande Git initialise un nouveau depot local ?', options: ['git new', 'git init', 'git create', 'git start'], correct: 1, explanation: '"git init" cree un nouveau depot Git dans le repertoire courant en initialisant le repertoire .git/ qui contient toute l\'historique et la configuration. On peut aussi utiliser "git init nom-dossier" pour creer un nouveau dossier avec le depot.' },
    { id: 'q110_2', question: 'Quelle commande Git affiche l\'etat des fichiers modifies, ajoutes et non suivis ?', options: ['git log', 'git status', 'git diff', 'git show'], correct: 1, explanation: '"git status" montre les fichiers modifies (non stages), les fichiers en staging area (prets a etre commites), et les fichiers non suivis (untracked). C\'est la commande la plus utilisee pour savoir ou l\'on en est avant un commit.' },
    { id: 'q110_3', question: 'Quelle est la difference entre "git merge" et "git rebase" ?', options: ['Ils font la meme chose', 'merge cree un commit de fusion preservant l\'historique, rebase reecrit l\'historique en deplacant les commits sur la branche cible', 'rebase est plus rapide que merge', 'merge supprime la branche source, pas rebase'], correct: 1, explanation: 'git merge cree un commit de fusion (merge commit) qui preserve les deux branches dans l\'historique. git rebase reecrit l\'historique en rejouant les commits de la branche courante sur la branche cible, creant un historique lineaire.' },
    { id: 'q110_4', question: 'Quelle commande Git ajoute tous les fichiers modifies a la staging area ?', options: ['git commit -a', 'git add .', 'git stage --all', 'git track .'], correct: 1, explanation: '"git add ." ajoute tous les fichiers modifies et nouveaux du repertoire courant a la staging area. "git add -A" fait la meme chose depuis la racine du depot. "git commit -a" ajoute et commite les fichiers deja suivis mais pas les nouveaux.' },
    { id: 'q110_5', question: 'Quelle commande affiche l\'historique des commits avec un graphe des branches ?', options: ['git history --graph', 'git log --oneline --graph --all', 'git show --branches', 'git commits --tree'], correct: 1, explanation: '"git log --oneline --graph --all" affiche un historique compact (--oneline), avec un graphe ASCII des branches (--graph) et toutes les branches (--all). C\'est tres utile pour visualiser la structure des branches et des merges.' },
    { id: 'q110_6', question: 'Quelle commande Git annule le dernier commit en conservant les modifications ?', options: ['git revert HEAD', 'git reset --soft HEAD~1', 'git undo', 'git uncommit'], correct: 1, explanation: '"git reset --soft HEAD~1" deplace le pointeur HEAD d\'un commit en arriere en gardant les modifications dans la staging area. --mixed (defaut) garde les modifications en working directory. --hard supprime tout (attention, destructif).' },
    { id: 'q110_7', question: 'Quel fichier Git permet d\'ignorer certains fichiers et repertoires ?', options: ['.gitconfig', '.gitignore', '.gitexclude', '.gitskip'], correct: 1, explanation: 'Le fichier .gitignore a la racine du depot liste les patterns de fichiers a ignorer (ex: node_modules/, *.log, .env). Il est versionne avec le depot. On peut aussi avoir un .gitignore global dans ~/.gitignore_global.' },
    { id: 'q110_8', question: 'Quelle commande cree une nouvelle branche et bascule dessus immediatement ?', options: ['git branch new && git checkout new', 'git checkout -b nouvelle-branche', 'git new-branch nouvelle-branche', 'git create branch nouvelle-branche'], correct: 1, explanation: '"git checkout -b nouvelle-branche" est un raccourci qui cree la branche et bascule dessus. Depuis Git 2.23, on peut aussi utiliser "git switch -c nouvelle-branche". La commande longue equivalente est "git branch nom && git checkout nom".' },
    { id: 'q110_9', question: 'Quelle commande Git sauvegarde temporairement les modifications en cours sans faire de commit ?', options: ['git save', 'git stash', 'git shelve', 'git park'], correct: 1, explanation: '"git stash" sauvegarde les modifications en cours dans une pile temporaire et restaure le working directory a l\'etat du dernier commit. "git stash pop" restaure les modifications. Utile pour changer de branche rapidement.' },
    { id: 'q110_10', question: 'Quelle strategie Git Flow utilise des branches feature, develop, release et hotfix ?', options: ['Trunk-based development', 'Git Flow', 'GitHub Flow', 'Forking workflow'], correct: 1, explanation: 'Git Flow est un modele de branching qui utilise : main (production), develop (integration), feature/* (nouvelles fonctionnalites), release/* (preparation release) et hotfix/* (corrections urgentes). Il est adapte aux releases planifiees.' }
  ],
  111: [
    { id: 'q111_1', question: 'Quelle est la difference principale entre GitHub et GitLab en termes d\'hebergement ?', options: ['GitHub est uniquement cloud, GitLab est uniquement self-hosted', 'GitHub est principalement cloud (github.com), GitLab est disponible en self-hosted (CE/EE) et cloud (gitlab.com)', 'Il n\'y a aucune difference', 'GitLab ne supporte pas Git'], correct: 1, explanation: 'GitLab offre une edition Community (CE) gratuite installable sur vos propres serveurs et une edition Enterprise (EE). GitHub propose GitHub Enterprise Server pour le self-hosting. GitLab est souvent prefere pour le self-hosting grace a son modele open-core.' },
    { id: 'q111_2', question: 'Qu\'est-ce qu\'une Pull Request (GitHub) ou Merge Request (GitLab) ?', options: ['Une commande pour telecharger du code', 'Une demande formelle pour fusionner les changements d\'une branche dans une autre avec revue de code', 'Un rapport de bug', 'Une demande d\'acces au depot'], correct: 1, explanation: 'Une PR/MR est un mecanisme collaboratif permettant de proposer des changements, d\'effectuer une revue de code, d\'executer des tests automatises (CI), et de fusionner apres approbation. C\'est central dans le workflow DevOps.' },
    { id: 'q111_3', question: 'Quelle commande configure un depot distant (remote) dans Git ?', options: ['git remote add origin URL', 'git connect origin URL', 'git link origin URL', 'git upstream set URL'], correct: 0, explanation: '"git remote add origin URL" ajoute un depot distant nomme "origin" (convention pour le depot principal). On peut avoir plusieurs remotes (ex: upstream pour un fork). "git remote -v" affiche les remotes configures.' },
    { id: 'q111_4', question: 'Comment proteger la branche main pour empecher les push directs ?', options: ['git config main --protected', 'Via les Branch Protection Rules dans les parametres du depot (GitHub/GitLab)', 'En supprimant la branche main', 'En mettant la branche en lecture seule dans .gitconfig'], correct: 1, explanation: 'Les Branch Protection Rules (GitHub) ou Protected Branches (GitLab) permettent d\'exiger des PR/MR, des revues obligatoires, des checks CI passes, et de bloquer les push directs et les force push sur les branches protegees.' },
    { id: 'q111_5', question: 'Qu\'est-ce qu\'un fork dans le contexte de GitHub/GitLab ?', options: ['Une copie locale d\'un depot', 'Une copie complete d\'un depot sur votre compte, permettant de contribuer sans avoir les droits d\'ecriture sur l\'original', 'Une branche speciale', 'Un clone en lecture seule'], correct: 1, explanation: 'Un fork cree une copie du depot sur votre compte. Vous pouvez y faire des modifications puis proposer un PR/MR vers le depot original. C\'est le workflow standard pour contribuer a des projets open-source.' },
    { id: 'q111_6', question: 'Quelle commande Git synchronise le depot local avec les derniers changements du depot distant ?', options: ['git download', 'git pull', 'git sync', 'git update'], correct: 1, explanation: '"git pull" est un raccourci pour "git fetch" (telecharger les changements) suivi de "git merge" (fusionner). "git pull --rebase" utilise rebase au lieu de merge pour un historique plus lineaire.' },
    { id: 'q111_7', question: 'Quel fichier GitLab definit le pipeline CI/CD du projet ?', options: ['Jenkinsfile', '.gitlab-ci.yml', '.github/workflows/main.yml', 'pipeline.yaml'], correct: 1, explanation: 'Le fichier .gitlab-ci.yml a la racine du depot definit les stages, jobs et scripts du pipeline CI/CD GitLab. Il est automatiquement detecte par GitLab Runner. .github/workflows/ est pour GitHub Actions, Jenkinsfile pour Jenkins.' },
    { id: 'q111_8', question: 'Qu\'est-ce qu\'un webhook dans le contexte de GitHub/GitLab ?', options: ['Un outil de debugging', 'Une notification HTTP automatique envoyee a une URL externe lorsqu\'un evenement se produit (push, PR, issue)', 'Un type de branche', 'Un plugin de navigateur'], correct: 1, explanation: 'Un webhook envoie une requete HTTP POST a une URL configuree lorsqu\'un evenement se produit dans le depot (push, creation de PR). C\'est utilise pour declencher des builds CI, des deploiements, des notifications Slack, etc.' },
    { id: 'q111_9', question: 'Quelle authentification est recommandee pour les operations Git avec GitHub ?', options: ['Mot de passe en clair', 'Cle SSH ou token d\'acces personnel (PAT)', 'Authentification anonyme', 'Cookie de session'], correct: 1, explanation: 'GitHub ne supporte plus l\'authentification par mot de passe pour les operations Git. Les cles SSH (ed25519 recommande) ou les Personal Access Tokens (PAT) sont les methodes recommandees. Le PAT a des scopes configurables pour limiter les permissions.' },
    { id: 'q111_10', question: 'Qu\'est-ce que le code review dans un workflow Git ?', options: ['La compilation automatique du code', 'L\'examen du code par des pairs avant la fusion pour ameliorer la qualite et partager les connaissances', 'Le test automatise du code', 'La documentation du code'], correct: 1, explanation: 'Le code review consiste a examiner les changements proposes dans une PR/MR. Les reviewers verifient la logique, la securite, les conventions, et partagent leurs connaissances. C\'est une pratique DevOps essentielle pour la qualite du code.' }
  ],
  112: [
    { id: 'q112_1', question: 'Quelle est la syntaxe correcte pour definir une fonction en Python ?', options: ['function ma_fonction():', 'def ma_fonction():', 'func ma_fonction():', 'fn ma_fonction():'], correct: 1, explanation: 'En Python, on definit une fonction avec le mot-cle "def" suivi du nom, des parentheses pour les parametres et de deux-points. Le corps est indente (4 espaces par convention PEP 8).' },
    { id: 'q112_2', question: 'Quel module Python standard est recommande pour executer des commandes shell depuis un script ?', options: ['shlex', 'subprocess', 'shell', 'commands'], correct: 1, explanation: 'Le module subprocess est le standard Python pour executer des commandes shell. subprocess.run() est recommande (Python 3.5+). Il permet de capturer stdout/stderr, gerer les codes de retour et eviter les injections de commandes.' },
    { id: 'q112_3', question: 'Comment lire un fichier JSON en Python ?', options: ['json.read("fichier.json")', 'import json; json.load(open("fichier.json"))', 'read_json("fichier.json")', 'parse_json("fichier.json")'], correct: 1, explanation: 'Le module json de la librairie standard permet de lire du JSON avec json.load() (depuis un fichier) ou json.loads() (depuis une string). Il est recommande d\'utiliser un context manager : with open("f.json") as f: data = json.load(f).' },
    { id: 'q112_4', question: 'Quel module Python est utilise pour faire des requetes HTTP ?', options: ['http', 'requests', 'urllib.request', 'httplib'], correct: 1, explanation: 'La librairie "requests" est le standard de facto pour les requetes HTTP en Python (pip install requests). Elle offre une API simple : requests.get(), requests.post(), etc. urllib.request est dans la librairie standard mais plus verbose.' },
    { id: 'q112_5', question: 'Comment gerer les erreurs en Python ?', options: ['if error then handle', 'try: ... except Exception as e: ...', 'catch(error) { ... }', 'on_error do ...'], correct: 1, explanation: 'Python utilise try/except pour la gestion des erreurs. "try" contient le code a proteger, "except" capture les exceptions. On peut specifier le type d\'exception (ValueError, FileNotFoundError) et acceder a l\'objet exception avec "as e".' },
    { id: 'q112_6', question: 'Quel outil Python gere les environnements virtuels pour isoler les dependances ?', options: ['pip', 'venv', 'conda seulement', 'virtualbox'], correct: 1, explanation: 'Le module venv (Python 3.3+) cree des environnements virtuels isoles : "python3 -m venv mon_env". On l\'active avec "source mon_env/bin/activate". Chaque venv a son propre pip et ses propres packages, evitant les conflits de dependances.' },
    { id: 'q112_7', question: 'Quel module Python permet de parser des fichiers YAML (utilises par Ansible, Docker Compose, Kubernetes) ?', options: ['json', 'yaml', 'PyYAML', 'configparser'], correct: 2, explanation: 'PyYAML (pip install pyyaml) est la librairie standard pour lire et ecrire du YAML en Python : yaml.safe_load() pour lire, yaml.dump() pour ecrire. Toujours utiliser safe_load() au lieu de load() pour eviter l\'execution de code arbitraire.' },
    { id: 'q112_8', question: 'Comment parcourir un dictionnaire Python avec ses cles et valeurs ?', options: ['for key, value in dict:', 'for key, value in dict.items():', 'for (key, value) of dict:', 'foreach key, value in dict:'], correct: 1, explanation: 'La methode .items() retourne des paires (cle, valeur) iterables. "for key in dict:" itere uniquement sur les cles. "for value in dict.values():" itere uniquement sur les valeurs.' },
    { id: 'q112_9', question: 'Quel outil formate automatiquement le code Python selon PEP 8 ?', options: ['pylint', 'black', 'mypy', 'pytest'], correct: 1, explanation: 'Black est un formateur de code Python opinionnaire qui reformate automatiquement le code selon un style coherent (base sur PEP 8). pylint est un linter (analyse statique), mypy un type checker, et pytest un framework de test.' },
    { id: 'q112_10', question: 'Quelle commande pip genere un fichier listant toutes les dependances installees ?', options: ['pip list > requirements.txt', 'pip freeze > requirements.txt', 'pip export > requirements.txt', 'pip save > requirements.txt'], correct: 1, explanation: '"pip freeze" affiche toutes les librairies installees avec leurs versions exactes au format "package==version". Rediriger vers requirements.txt permet de reproduire l\'environnement avec "pip install -r requirements.txt".' }
  ]
}
