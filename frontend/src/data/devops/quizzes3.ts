import type { QuizQuestion } from '../quizzes'

export const quizzes: Record<number, QuizQuestion[]> = {
  113: [
    { id: 'q113_1', question: 'Quelle est la difference fondamentale entre un conteneur et une machine virtuelle ?', options: ['Un conteneur est plus gros qu\'une VM', 'Un conteneur partage le noyau de l\'hote et isole les processus, une VM virtualise tout le materiel avec son propre noyau', 'Une VM est plus rapide qu\'un conteneur', 'Il n\'y a aucune difference'], correct: 1, explanation: 'Un conteneur utilise les namespaces et cgroups du noyau Linux pour isoler les processus sans noyau supplementaire. Une VM virtualise le materiel complet avec un hyperviseur et embarque son propre OS, ce qui la rend plus lourde mais plus isolee.' },
    { id: 'q113_2', question: 'Quelle technologie du noyau Linux fournit l\'isolation des ressources (CPU, memoire) pour les conteneurs ?', options: ['namespaces', 'cgroups', 'chroot', 'iptables'], correct: 1, explanation: 'Les cgroups (control groups) limitent et comptabilisent les ressources (CPU, memoire, I/O) utilisees par un groupe de processus. Les namespaces fournissent l\'isolation (PID, reseau, mount). Les deux ensemble forment la base des conteneurs.' },
    { id: 'q113_3', question: 'Quelle commande affiche tous les conteneurs Docker en cours d\'execution ?', options: ['docker list', 'docker ps', 'docker containers', 'docker show'], correct: 1, explanation: '"docker ps" affiche les conteneurs en cours d\'execution. "docker ps -a" affiche tous les conteneurs (y compris arretes). Chaque conteneur a un ID, un nom, une image source, un statut, des ports et une commande.' },
    { id: 'q113_4', question: 'Quelle commande lance un conteneur interactif avec un shell bash depuis l\'image Ubuntu ?', options: ['docker start ubuntu bash', 'docker run -it ubuntu bash', 'docker exec ubuntu bash', 'docker create ubuntu --shell'], correct: 1, explanation: '"docker run -it ubuntu bash" cree et demarre un conteneur (-i pour interactif, -t pour pseudo-TTY) avec bash. Sans -it, le conteneur se termine immediatement car bash n\'a pas de terminal attache.' },
    { id: 'q113_5', question: 'Quel composant Docker est responsable de l\'execution des conteneurs ?', options: ['Docker CLI', 'Docker Daemon (dockerd)', 'Docker Hub', 'Docker Compose'], correct: 1, explanation: 'Le Docker Daemon (dockerd) est le processus serveur qui gere les images, conteneurs, reseaux et volumes. Le Docker CLI communique avec le daemon via l\'API REST (socket Unix /var/run/docker.sock). Docker Hub est le registre d\'images public.' },
    { id: 'q113_6', question: 'Quelle commande arrete proprement un conteneur en cours d\'execution ?', options: ['docker kill container_id', 'docker stop container_id', 'docker terminate container_id', 'docker end container_id'], correct: 1, explanation: '"docker stop" envoie d\'abord SIGTERM au processus principal, lui laissant 10 secondes (par defaut) pour s\'arreter proprement, puis envoie SIGKILL. "docker kill" envoie directement SIGKILL sans delai de grace.' },
    { id: 'q113_7', question: 'Quelle commande supprime tous les conteneurs arretes, les images non utilisees et les volumes orphelins ?', options: ['docker clean all', 'docker system prune -a --volumes', 'docker remove --unused', 'docker gc --all'], correct: 1, explanation: '"docker system prune -a --volumes" nettoie tout : conteneurs arretes, images non referencees (-a inclut les images sans tag), les reseaux non utilises et les volumes orphelins (--volumes). C\'est utile pour liberer de l\'espace disque.' },
    { id: 'q113_8', question: 'Quel est le role de l\'image de base dans un conteneur Docker ?', options: ['Elle contient uniquement le code de l\'application', 'Elle fournit le systeme de fichiers racine avec l\'OS minimal, les librairies et outils necessaires', 'Elle stocke les donnees persistantes du conteneur', 'Elle configure le reseau du conteneur'], correct: 1, explanation: 'L\'image de base (ex: ubuntu, alpine, node) fournit le filesystem racine du conteneur avec un OS minimal et les librairies systeme. Les couches suivantes du Dockerfile ajoutent l\'application et ses dependances par-dessus cette base.' },
    { id: 'q113_9', question: 'Quelle commande execute une commande dans un conteneur deja en cours d\'execution ?', options: ['docker run container_id commande', 'docker exec -it container_id commande', 'docker attach container_id commande', 'docker enter container_id commande'], correct: 1, explanation: '"docker exec -it container_id commande" execute une nouvelle commande dans un conteneur existant. C\'est different de "docker run" qui cree un nouveau conteneur. "docker exec -it container bash" est utilise pour obtenir un shell de debug.' },
    { id: 'q113_10', question: 'Quelle commande affiche les logs d\'un conteneur en temps reel ?', options: ['docker log container_id', 'docker logs -f container_id', 'docker output container_id', 'docker stream container_id'], correct: 1, explanation: '"docker logs -f" (--follow) affiche les logs du conteneur et continue d\'afficher les nouvelles entrees en temps reel (comme tail -f). Sans -f, seuls les logs existants sont affiches. --tail N limite aux N dernieres lignes.' }
  ],
  114: [
    { id: 'q114_1', question: 'Quelle instruction Dockerfile definit l\'image de base ?', options: ['BASE ubuntu:22.04', 'FROM ubuntu:22.04', 'IMAGE ubuntu:22.04', 'USE ubuntu:22.04'], correct: 1, explanation: 'L\'instruction FROM est obligatoire et doit etre la premiere du Dockerfile (apres les ARG eventuels). Elle definit l\'image de base. On peut utiliser un tag specifique (ubuntu:22.04) ou "FROM scratch" pour une image vide.' },
    { id: 'q114_2', question: 'Quelle instruction Dockerfile execute une commande pendant la construction de l\'image ?', options: ['EXEC apt-get install nginx', 'RUN apt-get install -y nginx', 'CMD apt-get install nginx', 'DO apt-get install nginx'], correct: 1, explanation: 'RUN execute une commande pendant le build et cree une nouvelle couche dans l\'image. CMD definit la commande par defaut executee au demarrage du conteneur (pas pendant le build). L\'option -y pour apt-get est necessaire car le build est non-interactif.' },
    { id: 'q114_3', question: 'Quelle est la difference entre CMD et ENTRYPOINT dans un Dockerfile ?', options: ['Il n\'y a pas de difference', 'CMD definit la commande par defaut (remplacable), ENTRYPOINT definit l\'executable principal (non remplacable facilement)', 'ENTRYPOINT est obsolete', 'CMD est execute pendant le build, ENTRYPOINT au demarrage'], correct: 1, explanation: 'ENTRYPOINT definit l\'executable principal du conteneur (ex: ENTRYPOINT ["nginx"]). CMD fournit les arguments par defaut qui peuvent etre remplaces en ligne de commande. Ensemble : ENTRYPOINT ["python"] CMD ["app.py"] -> "python app.py" par defaut.' },
    { id: 'q114_4', question: 'Quelle instruction Dockerfile copie des fichiers du contexte de build dans l\'image ?', options: ['ADD . /app', 'COPY . /app', 'PUT . /app', 'IMPORT . /app'], correct: 1, explanation: 'COPY copie des fichiers/repertoires du contexte de build dans l\'image. ADD fait pareil mais avec des fonctionnalites supplementaires (decompression tar, telechargement URL). La bonne pratique est d\'utiliser COPY sauf si on a besoin des extras d\'ADD.' },
    { id: 'q114_5', question: 'Pourquoi combiner plusieurs commandes RUN en une seule instruction avec && ?', options: ['Pour des raisons de securite', 'Pour reduire le nombre de couches dans l\'image et sa taille finale', 'Pour accelerer l\'execution des commandes', 'Pour eviter les erreurs de syntaxe'], correct: 1, explanation: 'Chaque instruction RUN cree une nouvelle couche. Combiner avec && (ex: RUN apt-get update && apt-get install -y nginx && rm -rf /var/lib/apt/lists/*) reduit le nombre de couches et permet de supprimer les fichiers temporaires dans la meme couche.' },
    { id: 'q114_6', question: 'Quel est le role du fichier .dockerignore ?', options: ['Il liste les images a ignorer lors du pull', 'Il exclut des fichiers du contexte de build envoye au daemon Docker', 'Il configure les reseaux Docker a ignorer', 'Il liste les conteneurs a ne pas demarrer'], correct: 1, explanation: 'Le fichier .dockerignore fonctionne comme .gitignore : il exclut des fichiers du contexte de build (node_modules, .git, .env). Cela reduit la taille du contexte, accelere le build et evite de copier des fichiers sensibles.' },
    { id: 'q114_7', question: 'Quelle instruction Dockerfile expose un port sans le publier sur l\'hote ?', options: ['PORT 3000', 'EXPOSE 3000', 'LISTEN 3000', 'OPEN 3000'], correct: 1, explanation: 'EXPOSE documente le port sur lequel l\'application ecoute. C\'est informatif et ne publie pas le port sur l\'hote. Pour publier, il faut utiliser "docker run -p 8080:3000" ou "-P" (publie tous les ports EXPOSE sur des ports aleatoires).' },
    { id: 'q114_8', question: 'Quelle technique de build multi-stage reduit la taille de l\'image finale ?', options: ['Utiliser une seule instruction RUN', 'Utiliser plusieurs FROM dans le Dockerfile pour separer le build de l\'image finale', 'Compresser l\'image avec gzip', 'Supprimer le Dockerfile apres le build'], correct: 1, explanation: 'Le multi-stage build utilise plusieurs FROM : le premier stage compile l\'application (avec les outils de build), le second stage copie uniquement les artefacts necessaires dans une image minimale. Exemple : build avec node:18, deploiement avec nginx:alpine.' },
    { id: 'q114_9', question: 'Quelle instruction Dockerfile definit le repertoire de travail pour les instructions suivantes ?', options: ['DIR /app', 'WORKDIR /app', 'CD /app', 'CHDIR /app'], correct: 1, explanation: 'WORKDIR definit le repertoire de travail pour les instructions RUN, CMD, ENTRYPOINT, COPY et ADD suivantes. Si le repertoire n\'existe pas, il est cree automatiquement. C\'est preferable a "RUN mkdir /app && cd /app".' },
    { id: 'q114_10', question: 'Quelle commande construit une image Docker a partir d\'un Dockerfile ?', options: ['docker create -t mon-image .', 'docker build -t mon-image .', 'docker make -t mon-image .', 'docker compile -t mon-image .'], correct: 1, explanation: '"docker build -t mon-image ." construit une image depuis le Dockerfile du repertoire courant (.) avec le tag "mon-image". Le point est le contexte de build. -f permet de specifier un Dockerfile different.' }
  ],
  115: [
    { id: 'q115_1', question: 'Quel fichier definit les services, reseaux et volumes d\'une application multi-conteneurs avec Docker Compose ?', options: ['docker-compose.conf', 'docker-compose.yml', 'compose.json', 'services.yaml'], correct: 1, explanation: 'Le fichier docker-compose.yml (ou compose.yml depuis Docker Compose V2) definit l\'ensemble des services, reseaux et volumes. Il utilise la syntaxe YAML et permet de demarrer tout l\'environnement avec une seule commande.' },
    { id: 'q115_2', question: 'Quelle commande Docker Compose demarre tous les services en arriere-plan ?', options: ['docker-compose start -d', 'docker-compose up -d', 'docker-compose run --detach', 'docker-compose deploy'], correct: 1, explanation: '"docker-compose up -d" (ou "docker compose up -d" en V2) cree les conteneurs, reseaux et volumes, puis demarre les services en mode detache (-d). Sans -d, les logs s\'affichent dans le terminal.' },
    { id: 'q115_3', question: 'Comment definir une dependance entre services dans Docker Compose ?', options: ['requires: [db]', 'depends_on: [db]', 'needs: [db]', 'after: [db]'], correct: 1, explanation: 'La directive "depends_on" definit l\'ordre de demarrage. Attention : depends_on attend uniquement le demarrage du conteneur, pas que l\'application soit prete (utiliser healthcheck avec condition: service_healthy).' },
    { id: 'q115_4', question: 'Quelle directive Docker Compose definit les variables d\'environnement d\'un service ?', options: ['vars:', 'environment:', 'env:', 'config:'], correct: 1, explanation: 'La directive "environment:" permet de definir les variables d\'environnement sous forme de liste (- VAR=valeur) ou de mapping (VAR: valeur). On peut aussi utiliser "env_file:" pour charger les variables depuis un fichier .env.' },
    { id: 'q115_5', question: 'Quelle commande Docker Compose arrete et supprime les conteneurs, reseaux et volumes ?', options: ['docker-compose stop --remove', 'docker-compose down -v', 'docker-compose destroy', 'docker-compose clean'], correct: 1, explanation: '"docker-compose down" arrete et supprime les conteneurs et reseaux. L\'option -v supprime aussi les volumes nommes. "--rmi all" supprime les images. "docker-compose stop" arrete les conteneurs sans les supprimer.' },
    { id: 'q115_6', question: 'Comment monter un volume nomme pour la persistance des donnees dans Docker Compose ?', options: ['storage: db_data:/var/lib/mysql', 'volumes: - db_data:/var/lib/mysql (et declarer db_data dans la section volumes: racine)', 'mount: db_data:/var/lib/mysql', 'disk: db_data:/var/lib/mysql'], correct: 1, explanation: 'On declare un volume nomme au niveau du service (volumes: - db_data:/var/lib/mysql) et au niveau racine du compose (volumes: db_data:). Les volumes nommes persistent meme apres "docker-compose down" (sauf avec -v).' },
    { id: 'q115_7', question: 'Quelle commande affiche les logs de tous les services Docker Compose en temps reel ?', options: ['docker-compose output -f', 'docker-compose logs -f', 'docker-compose stream', 'docker-compose tail -f'], correct: 1, explanation: '"docker-compose logs -f" suit les logs de tous les services en temps reel. On peut filtrer par service : "docker-compose logs -f web". L\'option --tail=N limite aux N dernieres lignes par service.' },
    { id: 'q115_8', question: 'Comment definir un healthcheck pour un service dans Docker Compose ?', options: ['check: curl http://localhost:3000', 'healthcheck: test: curl -f http://localhost:3000 (avec interval, timeout, retries)', 'monitor: http://localhost:3000', 'probe: http://localhost:3000'], correct: 1, explanation: 'Le healthcheck definit un test de sante avec : test (commande), interval (frequence), timeout (delai max), retries (echecs avant "unhealthy"), start_period (delai initial). depends_on avec condition: service_healthy attend le healthcheck.' },
    { id: 'q115_9', question: 'Quelle directive Docker Compose limite les ressources CPU et memoire d\'un service ?', options: ['resources:', 'deploy: resources: limits:', 'limits:', 'constraints:'], correct: 1, explanation: 'Sous "deploy: resources: limits:" on definit cpus (ex: "0.5") et memory (ex: "512M"). En mode Docker Compose standard (non Swarm), on peut aussi utiliser "mem_limit" et "cpus" directement selon la version.' },
    { id: 'q115_10', question: 'Quelle commande reconstruit les images des services avant de les demarrer ?', options: ['docker-compose rebuild', 'docker-compose up --build', 'docker-compose build --restart', 'docker-compose create --rebuild'], correct: 1, explanation: '"docker-compose up --build" force la reconstruction des images avant de demarrer les services. Sans --build, Docker Compose reutilise les images en cache. "docker-compose build" reconstruit sans demarrer.' }
  ],
  116: [
    { id: 'q116_1', question: 'Quelle est la difference entre un volume Docker et un bind mount ?', options: ['Il n\'y a pas de difference', 'Un volume est gere par Docker dans /var/lib/docker/volumes/, un bind mount monte un chemin arbitraire de l\'hote', 'Un bind mount est plus performant', 'Un volume ne persiste pas les donnees'], correct: 1, explanation: 'Les volumes Docker sont geres par Docker et sont portables. Les bind mounts montent un chemin specifique de l\'hote dans le conteneur, utiles pour le developpement (partage de code source). Les volumes sont recommandes pour la production.' },
    { id: 'q116_2', question: 'Quelle commande cree un reseau Docker bridge personnalise ?', options: ['docker net create mon-reseau', 'docker network create mon-reseau', 'docker create-network mon-reseau', 'docker bridge create mon-reseau'], correct: 1, explanation: '"docker network create mon-reseau" cree un reseau bridge personnalise. Contrairement au bridge par defaut, un reseau personnalise offre la resolution DNS automatique par nom de conteneur et une meilleure isolation.' },
    { id: 'q116_3', question: 'Quel type de reseau Docker permet aux conteneurs de communiquer entre plusieurs hotes ?', options: ['bridge', 'host', 'overlay', 'none'], correct: 2, explanation: 'Le reseau overlay cree un reseau virtuel qui s\'etend sur plusieurs hotes Docker (Swarm ou driver externe). Les conteneurs sur differents hotes peuvent communiquer comme s\'ils etaient sur le meme reseau local.' },
    { id: 'q116_4', question: 'Comment les conteneurs sur un meme reseau Docker personnalise se trouvent-ils entre eux ?', options: ['Par adresse IP uniquement', 'Par resolution DNS automatique utilisant le nom du conteneur', 'Par broadcast ARP', 'Ils ne peuvent pas communiquer'], correct: 1, explanation: 'Docker fournit un serveur DNS embarque (127.0.0.11) qui resout les noms de conteneurs en adresses IP sur les reseaux personnalises. On peut utiliser "docker run --name mon-app" puis "ping mon-app" depuis un autre conteneur du meme reseau.' },
    { id: 'q116_5', question: 'Quelle option "docker run" monte un volume nomme dans un conteneur ?', options: ['-v mon-volume:/data', '--mount source=mon-volume,target=/data', 'Les deux syntaxes sont valides', '--volume-attach mon-volume:/data'], correct: 2, explanation: 'Les deux syntaxes sont valides : "-v mon-volume:/data" (syntaxe courte) et "--mount source=mon-volume,target=/data" (syntaxe longue, plus explicite). Si le volume n\'existe pas, Docker le cree automatiquement avec -v.' },
    { id: 'q116_6', question: 'Quel type de reseau Docker supprime toute isolation reseau et utilise directement l\'interface de l\'hote ?', options: ['bridge', 'host', 'overlay', 'macvlan'], correct: 1, explanation: 'Le reseau "host" supprime l\'isolation reseau : le conteneur partage directement la stack reseau de l\'hote. Pas besoin de -p pour publier les ports. C\'est plus performant mais moins isole.' },
    { id: 'q116_7', question: 'Quelle commande liste les volumes Docker existants ?', options: ['docker volumes', 'docker volume ls', 'docker list volumes', 'docker show volumes'], correct: 1, explanation: '"docker volume ls" affiche tous les volumes Docker. "docker volume inspect nom" montre les details (point de montage, driver). "docker volume prune" supprime les volumes non utilises par aucun conteneur.' },
    { id: 'q116_8', question: 'Quelle option docker run publie le port 80 du conteneur sur le port 8080 de l\'hote ?', options: ['-p 80:8080', '-p 8080:80', '--port 8080=80', '-expose 80:8080'], correct: 1, explanation: 'La syntaxe est "-p port_hote:port_conteneur". Donc "-p 8080:80" publie le port 80 du conteneur sur le port 8080 de l\'hote. On peut aussi specifier l\'interface : "-p 127.0.0.1:8080:80" pour n\'ecouter que sur localhost.' },
    { id: 'q116_9', question: 'Comment sauvegarder les donnees d\'un volume Docker ?', options: ['docker volume backup nom', 'En utilisant un conteneur temporaire qui monte le volume et copie les donnees avec tar', 'docker export volume nom', 'Les volumes sont automatiquement sauvegardes'], correct: 1, explanation: 'Docker n\'a pas de commande native de backup de volume. On utilise un conteneur temporaire : "docker run --rm -v mon-volume:/data -v $(pwd):/backup alpine tar czf /backup/backup.tar.gz /data".' },
    { id: 'q116_10', question: 'Quel reseau Docker attribue une adresse MAC unique a chaque conteneur, le faisant apparaitre comme un appareil physique ?', options: ['bridge', 'overlay', 'macvlan', 'host'], correct: 2, explanation: 'Le driver macvlan attribue une adresse MAC unique a chaque conteneur, lui permettant d\'apparaitre comme un appareil physique sur le reseau local. C\'est utile pour les applications necessitant une adresse IP directe sur le reseau physique.' }
  ],
  117: [
    { id: 'q117_1', question: 'Pourquoi est-il recommande d\'utiliser un utilisateur non-root dans un conteneur Docker ?', options: ['Pour accelerer le conteneur', 'Pour reduire la surface d\'attaque en cas de compromission du conteneur', 'Pour economiser de la memoire', 'Pour ameliorer la compatibilite'], correct: 1, explanation: 'Executer en root dans un conteneur signifie que si un attaquant s\'echappe du conteneur, il peut avoir les droits root sur l\'hote. L\'instruction USER dans le Dockerfile definit un utilisateur non-root pour minimiser ce risque.' },
    { id: 'q117_2', question: 'Quelle instruction Dockerfile definit l\'utilisateur sous lequel les commandes suivantes s\'executent ?', options: ['RUNAS devops', 'USER devops', 'SETUSER devops', 'IDENTITY devops'], correct: 1, explanation: '"USER devops" change l\'utilisateur pour les instructions RUN, CMD et ENTRYPOINT suivantes. Il faut d\'abord creer l\'utilisateur avec "RUN addgroup -S app && adduser -S app -G app" (Alpine) ou "useradd" (Debian).' },
    { id: 'q117_3', question: 'Qu\'est-ce que Docker Content Trust (DCT) ?', options: ['Un outil de monitoring', 'Un mecanisme de signature et verification des images Docker pour garantir leur integrite', 'Un pare-feu pour conteneurs', 'Un outil de scan de vulnerabilites'], correct: 1, explanation: 'Docker Content Trust utilise Notary pour signer les images lors du push et verifier les signatures lors du pull. Active avec DOCKER_CONTENT_TRUST=1, il garantit que les images n\'ont pas ete modifiees entre le push et le pull.' },
    { id: 'q117_4', question: 'Quel outil open-source scanne les images Docker pour detecter les vulnerabilites connues ?', options: ['Docker Scout', 'Trivy', 'Docker Bench', 'Trivy et Docker Scout'], correct: 3, explanation: 'Trivy (Aqua Security) et Docker Scout sont des scanners de vulnerabilites pour images Docker. Trivy est open-source et largement utilise en CI/CD. Ils analysent les CVE dans les paquets OS et les dependances applicatives.' },
    { id: 'q117_5', question: 'Quelle bonne pratique reduit la surface d\'attaque d\'une image Docker ?', options: ['Utiliser l\'image ubuntu:latest comme base', 'Utiliser une image minimale (alpine, distroless) et n\'installer que le strict necessaire', 'Installer tous les outils de debug dans l\'image', 'Utiliser toujours le tag latest'], correct: 1, explanation: 'Les images minimales (Alpine ~5 Mo, distroless ~2 Mo) contiennent moins de paquets et donc moins de vulnerabilites. N\'installer que le necessaire et utiliser le multi-stage build reduisent encore la surface d\'attaque.' },
    { id: 'q117_6', question: 'Que sont les Linux capabilities dans le contexte de la securite des conteneurs ?', options: ['Des plugins Docker', 'Des permissions fines qui decomposent les privileges root en unites plus granulaires', 'Des extensions du noyau', 'Des limites de ressources'], correct: 1, explanation: 'Les capabilities Linux decomposent les privileges root en unites fines (NET_BIND_SERVICE, SYS_ADMIN, NET_RAW). Docker supprime certaines capabilities par defaut. --cap-drop ALL --cap-add NET_BIND_SERVICE applique le principe du moindre privilege.' },
    { id: 'q117_7', question: 'Quel flag docker run empeche un conteneur d\'obtenir de nouveaux privileges ?', options: ['--no-root', '--security-opt no-new-privileges', '--read-only', '--cap-drop ALL'], correct: 1, explanation: '"--security-opt no-new-privileges" empeche les processus d\'obtenir de nouveaux privileges (via SUID, sudo). Combine avec USER non-root et --cap-drop ALL pour une securite maximale.' },
    { id: 'q117_8', question: 'Pourquoi ne pas stocker de secrets (mots de passe, cles API) dans un Dockerfile ?', options: ['Cela ralentit le build', 'Les Dockerfile sont souvent versionnes et les couches d\'image sont accessibles, exposant les secrets', 'Docker refuse les secrets dans les Dockerfile', 'Les secrets ne fonctionnent pas dans les conteneurs'], correct: 1, explanation: 'Les secrets dans un Dockerfile sont visibles dans l\'historique Git et les couches de l\'image (docker history). Utiliser des variables d\'environnement, Docker Secrets (Swarm), des fichiers montes, ou un gestionnaire de secrets (Vault).' },
    { id: 'q117_9', question: 'Que fait l\'option --read-only de docker run ?', options: ['Le conteneur ne peut pas lire les fichiers', 'Le systeme de fichiers du conteneur est en lecture seule, empechant toute ecriture', 'Les logs sont desactives', 'Le reseau est en lecture seule'], correct: 1, explanation: '"--read-only" monte le filesystem du conteneur en lecture seule. Le conteneur ne peut ecrire que dans les volumes montes et les tmpfs. Un attaquant ne peut pas modifier les binaires du systeme.' },
    { id: 'q117_10', question: 'Quel est le role de Docker Bench for Security ?', options: ['Il benchmark les performances des conteneurs', 'Il verifie la configuration Docker selon les bonnes pratiques du CIS Benchmark', 'Il gere les certificats SSL', 'Il monitore les conteneurs en temps reel'], correct: 1, explanation: 'Docker Bench for Security est un script qui audite la configuration Docker selon le CIS Docker Benchmark. Il verifie la config du daemon, des conteneurs, des images, du reseau et des volumes, et produit un rapport avec des recommandations.' }
  ],
  118: [
    { id: 'q118_1', question: 'Quelle strategie de restart Docker est recommandee pour les services en production ?', options: ['no', 'always', 'unless-stopped', 'on-failure'], correct: 2, explanation: '"unless-stopped" redemarre automatiquement le conteneur sauf s\'il a ete arrete manuellement. "always" redemarre meme apres un arret manuel (moins pratique pour la maintenance). "on-failure" ne redemarre que sur les codes d\'erreur.' },
    { id: 'q118_2', question: 'Comment limiter la memoire d\'un conteneur Docker a 512 Mo ?', options: ['docker run --ram 512m', 'docker run --memory 512m', 'docker run --mem-limit 512m', 'docker run -m 512m uniquement'], correct: 1, explanation: '"docker run --memory 512m" (ou -m 512m) limite la memoire a 512 Mo. Si le conteneur depasse cette limite, Docker l\'arrete (OOM killed). On peut ajouter --memory-swap pour limiter aussi le swap.' },
    { id: 'q118_3', question: 'Quel est le role d\'un registre d\'images Docker prive ?', options: ['Heberger le code source', 'Stocker et distribuer des images Docker de maniere privee au sein de l\'organisation', 'Gerer les certificats SSL', 'Monitorer les conteneurs'], correct: 1, explanation: 'Un registre prive (Harbor, GitLab Container Registry, AWS ECR, Azure ACR) stocke les images de maniere securisee. Cela evite de publier sur Docker Hub et permet le controle d\'acces, le scan de vulnerabilites et la gestion des politiques.' },
    { id: 'q118_4', question: 'Quelle commande Docker tag une image et la pousse vers un registre prive ?', options: ['docker push mon-image:latest mon-registre/', 'docker tag mon-image:latest registry.example.com/mon-image:latest && docker push registry.example.com/mon-image:latest', 'docker upload mon-image registry.example.com', 'docker deploy mon-image registry.example.com'], correct: 1, explanation: 'Il faut d\'abord tagger l\'image avec le nom complet du registre (docker tag), puis la pousser (docker push). Le format est : registre/repository:tag. Pour Docker Hub, le registre est implicite.' },
    { id: 'q118_5', question: 'Quelle bonne pratique de logging est recommandee pour les conteneurs en production ?', options: ['Ecrire les logs dans des fichiers a l\'interieur du conteneur', 'Envoyer les logs sur stdout/stderr et utiliser un driver de logging ou un collecteur externe', 'Desactiver les logs pour les performances', 'Stocker les logs dans un volume partage'], correct: 1, explanation: 'La bonne pratique "12-factor app" est d\'envoyer les logs sur stdout/stderr. Docker les capture via son driver de logging (json-file, fluentd, syslog, awslogs). Un collecteur externe (Fluentd, Filebeat) les achemine vers un systeme centralise.' },
    { id: 'q118_6', question: 'Que permet l\'option --init de docker run ?', options: ['Initialise un nouveau reseau', 'Ajoute un processus init (tini) comme PID 1 pour gerer les signaux et reaper les processus zombies', 'Initialise les volumes', 'Configure l\'environnement initial'], correct: 1, explanation: 'Sans --init, le processus applicatif est PID 1 et doit gerer les signaux Unix et le reaping des processus zombies. --init ajoute tini comme PID 1, qui transmet les signaux et nettoie les processus enfants termines.' },
    { id: 'q118_7', question: 'Quelle commande Docker inspecte les details complets d\'un conteneur (config, reseau, mounts) ?', options: ['docker details container_id', 'docker inspect container_id', 'docker info container_id', 'docker describe container_id'], correct: 1, explanation: '"docker inspect" retourne un JSON detaille avec la configuration complete : image, volumes, reseaux, variables d\'environnement, etat, IP. On peut filtrer avec --format pour extraire une valeur specifique.' },
    { id: 'q118_8', question: 'Quelle strategie de mise a jour permet de deployer sans interruption de service ?', options: ['Arreter l\'ancien puis demarrer le nouveau', 'Deploiement blue-green : demarrer le nouveau, tester, puis basculer le trafic', 'Supprimer tous les conteneurs et recreer', 'Mettre a jour l\'image en place dans le conteneur'], correct: 1, explanation: 'Le deploiement blue-green demarre la nouvelle version (green) a cote de l\'ancienne (blue), teste, puis bascule le trafic via le reverse proxy. Le rolling update remplace progressivement les instances une par une.' },
    { id: 'q118_9', question: 'Comment surveiller les ressources (CPU, memoire) d\'un conteneur en temps reel ?', options: ['docker top container_id', 'docker stats', 'docker monitor', 'docker resources'], correct: 1, explanation: '"docker stats" affiche en temps reel la consommation CPU, memoire, I/O reseau et disque de tous les conteneurs. "docker stats container_id" filtre par conteneur. C\'est l\'equivalent de "top" pour les conteneurs.' },
    { id: 'q118_10', question: 'Quelle commande permet d\'exporter le filesystem d\'un conteneur en archive tar ?', options: ['docker save container_id > backup.tar', 'docker export container_id > backup.tar', 'docker backup container_id backup.tar', 'docker archive container_id backup.tar'], correct: 1, explanation: '"docker export" exporte le filesystem d\'un conteneur en archive tar (pas les volumes). "docker save" exporte une image avec ses couches. Pour les donnees, preferer la sauvegarde des volumes separement.' }
  ]
}
