import type { Flashcard } from '../flashcardsData'

export const flashcardData: Flashcard[] = [
  // ============================================================
  // === Linux (~18 cards) ===
  // ============================================================
  { id: 'df1', category: 'Linux', front: 'Which command displays running processes with their PID, CPU, and memory usage?', back: 'top (dynamic display) or ps aux (static snapshot). htop is a more readable alternative with an interactive interface.' },
  { id: 'df2', category: 'Linux', front: 'What is the difference between a hard link and a symbolic link (symlink)?', back: 'Hard link: points to the same inode, cannot cross filesystem boundaries. Symlink (ln -s): points to the file path, can cross filesystems and point to directories.' },
  { id: 'df3', category: 'Linux', front: 'How do you make a Bash script executable and run it?', back: 'chmod +x script.sh then ./script.sh. The first line must be the shebang: #!/bin/bash (or #!/usr/bin/env bash for better portability).' },
  { id: 'df4', category: 'Linux', front: 'What does chmod 755 file do?', back: 'Owner: rwx (7), Group: r-x (5), Others: r-x (5). The owner can do everything; others can read and execute but not modify.' },
  { id: 'df5', category: 'Linux', front: 'What is the difference between systemctl enable and systemctl start?', back: 'start launches the service immediately. enable configures the service to start automatically at boot (creates a symbolic link in systemd targets). Both are often used together.' },
  { id: 'df6', category: 'Linux', front: 'How do you view logs for a service with journald?', back: 'journalctl -u service-name. Useful options: -f (follow in real time), --since "1 hour ago", -p err (filter by priority), -b (since last boot).' },
  { id: 'df7', category: 'Linux', front: 'What does the command grep -r "pattern" /path/ do?', back: 'Recursively searches for the pattern in all files within the directory. Useful options: -i (case-insensitive), -n (show line numbers), -l (show only file names).' },
  { id: 'df8', category: 'Linux', front: 'Which command shows disk space usage?', back: 'df -h (space per filesystem, -h for human-readable). du -sh /path/ (directory size). ncdu is an interactive alternative.' },
  { id: 'df9', category: 'Linux', front: 'What does the /etc/passwd file contain?', back: 'The list of user accounts: login:x:UID:GID:comment:home:shell. The x indicates that the password is stored in /etc/shadow (accessible only by root).' },
  { id: 'df10', category: 'Linux', front: 'How do you add a user to a group in Linux?', back: 'usermod -aG group_name username. The -a (append) flag is crucial: without it, the user is removed from all other groups. Verify with: groups username.' },
  { id: 'df11', category: 'Linux', front: 'What does the pipe (|) do in a Linux command?', back: 'It redirects the standard output (stdout) of one command to the standard input (stdin) of the next. Example: cat file.log | grep ERROR | wc -l counts lines containing ERROR.' },
  { id: 'df12', category: 'Linux', front: 'What is the difference between > and >> in redirection?', back: '> overwrites the existing file with the output. >> appends the output to the end of the file without overwriting. 2> redirects stderr, &> redirects both stdout and stderr.' },
  { id: 'df13', category: 'Linux', front: 'How do you schedule a task with crontab?', back: 'crontab -e to edit, syntax: minute hour day_of_month month day_of_week command. Example: 0 3 * * * /backup.sh = every day at 3:00 AM. crontab -l to list.' },
  { id: 'df14', category: 'Linux', front: 'What does the command find / -name "*.log" -mtime +30 -delete do?', back: 'Searches from / for all .log files modified more than 30 days ago and deletes them. -mtime +30 = more than 30 days, -delete = deletion.' },
  { id: 'df15', category: 'Linux', front: 'How do you generate an SSH key pair?', back: 'ssh-keygen -t ed25519 -C "email@example.com". Creates a private key (~/.ssh/id_ed25519) and public key (~/.ssh/id_ed25519.pub). The public key is added to ~/.ssh/authorized_keys on the server.' },
  { id: 'df16', category: 'Linux', front: 'What does the /etc/sudoers file do?', back: 'Defines sudo rights: which users/groups can execute which commands with which privileges. Edited with visudo (checks syntax). Typical rule: user ALL=(ALL) ALL.' },
  { id: 'df17', category: 'Linux', front: 'How do you check which ports are listening on a Linux server?', back: 'ss -tlnp or netstat -tlnp. -t = TCP, -l = listening, -n = numeric, -p = PID/process. Shows which process is listening on which port.' },
  { id: 'df18', category: 'Linux', front: 'What is the difference between a foreground and background process?', back: 'Foreground: blocks the terminal, receives keyboard signals (Ctrl+C). Background: runs behind the scenes (command &). Ctrl+Z suspends, bg resumes in background, fg brings back to foreground.' },

  // ============================================================
  // === Git (~8 cards) ===
  // ============================================================
  { id: 'df19', category: 'Git', front: 'What is the difference between git merge and git rebase?', back: 'merge creates a merge commit and preserves the full history. rebase reapplies commits onto the target branch for a linear history. Rule: never rebase a public/shared branch.' },
  { id: 'df20', category: 'Git', front: 'How do you undo the last commit without losing changes?', back: 'git reset --soft HEAD~1 (keeps files in staging). git reset --mixed HEAD~1 (keeps files in working directory). git reset --hard HEAD~1 deletes changes (dangerous).' },
  { id: 'df21', category: 'Git', front: 'What is the .gitignore file for?', back: 'Defines files and directories to exclude from Git tracking. Patterns: *.log (all .log files), node_modules/ (directory), !important.log (exception). One .gitignore at root and per directory.' },
  { id: 'df22', category: 'Git', front: 'What is the difference between git fetch and git pull?', back: 'git fetch downloads remote commits without integrating them. git pull = git fetch + git merge (or rebase with --rebase). fetch is safer because it does not modify the local branch.' },
  { id: 'df23', category: 'Git', front: 'How does the GitFlow model work?', back: 'Branches: main (production), develop (integration), feature/* (features), release/* (release prep), hotfix/* (urgent fixes). Suited for planned releases.' },
  { id: 'df24', category: 'Git', front: 'What does git stash do and when should you use it?', back: 'Temporarily saves uncommitted changes. Useful for switching branches quickly. git stash pop restores and removes the stash, git stash apply restores without removing.' },
  { id: 'df25', category: 'Git', front: 'What is Semantic Versioning (SemVer)?', back: 'Format: MAJOR.MINOR.PATCH. MAJOR = incompatible changes (breaking changes). MINOR = new backward-compatible features. PATCH = bug fixes. Example: 2.4.1 -> 3.0.0 (breaking).' },
  { id: 'df26', category: 'Git', front: 'How do you create and push a Git tag?', back: 'git tag -a v1.0.0 -m "Version 1.0.0" (annotated tag). git push origin v1.0.0 (push the tag). git push origin --tags (push all tags). Tags are often used to trigger CI/CD pipelines.' },

  // ============================================================
  // === Docker (~16 cards) ===
  // ============================================================
  { id: 'df27', category: 'Docker', front: 'What is the difference between a container and a virtual machine?', back: 'A container shares the host kernel (namespaces + cgroups), starts in seconds, and uses little RAM. A VM includes a full OS with its own kernel via a hypervisor, heavier but more isolated.' },
  { id: 'df28', category: 'Docker', front: 'Which command runs an Nginx container in the background on port 8080?', back: 'docker run -d -p 8080:80 --name my-nginx nginx. -d = detached (background), -p 8080:80 = maps host port 8080 to container port 80.' },
  { id: 'df29', category: 'Docker', front: 'What is the difference between CMD and ENTRYPOINT in a Dockerfile?', back: 'CMD defines the default command (overridable at docker run). ENTRYPOINT defines the fixed command. Combination: ENTRYPOINT ["python"] CMD ["app.py"] lets you change the argument but not the command.' },
  { id: 'df30', category: 'Docker', front: 'What is a multi-stage build and why use it?', back: 'A technique with multiple FROM instructions in a Dockerfile. The first stage compiles/builds, the second only copies the final binary. Result: much smaller production image (no compiler, dev deps).' },
  { id: 'df31', category: 'Docker', front: 'How do you optimize Docker layer caching?', back: 'Order instructions from least to most frequently changed. COPY package.json + RUN npm install BEFORE COPY . (code changes more often than deps). Cache is invalidated from the first modified layer onward.' },
  { id: 'df32', category: 'Docker', front: 'What is the difference between a Docker volume and a bind mount?', back: 'Volume: managed by Docker (/var/lib/docker/volumes/), portable and performant. Bind mount: maps a host path (-v /host/path:/container/path), useful in dev for hot reload.' },
  { id: 'df33', category: 'Docker', front: 'What are the default Docker network drivers?', back: 'bridge (default, communication between containers on the same host), host (shares the host network, no isolation), none (no network), overlay (multi-host communication, Docker Swarm/K8s).' },
  { id: 'df34', category: 'Docker', front: 'How do you view Docker container logs?', back: 'docker logs container_name. Options: -f (follow in real time like tail -f), --tail 100 (last 100 lines), --since 1h (since 1 hour ago). Logs are stdout/stderr of the main process.' },
  { id: 'df35', category: 'Docker', front: 'What does docker-compose up -d --build do?', back: 'Rebuilds the images (--build) and starts all services defined in docker-compose.yml in the background (-d). Without --build, Docker reuses cached images even if the Dockerfile has changed.' },
  { id: 'df36', category: 'Docker', front: 'How do you limit a Docker container\'s resources?', back: 'docker run --memory=512m --cpus=0.5 image. In Compose: deploy.resources.limits. In K8s: resources.limits. Kernel cgroups enforce these limits. OOM killer terminates the container if memory is exceeded.' },
  { id: 'df37', category: 'Docker', front: 'Why should you not run a container as root?', back: 'A root process inside the container is root on the host in case of a container escape. Best practices: USER instruction in the Dockerfile, Docker rootless mode, securityContext runAsNonRoot in K8s.' },
  { id: 'df38', category: 'Docker', front: 'What is Docker Scout / Trivy for images?', back: 'Scanning tools that analyze image layers to detect CVEs (known vulnerabilities) in the base OS and dependencies. Should be integrated into the CI pipeline to block vulnerable images.' },
  { id: 'df39', category: 'Docker', front: 'What is the difference between COPY and ADD in a Dockerfile?', back: 'COPY copies local files into the image. ADD does the same but can also download from a URL and automatically extract .tar.gz archives. Best practice: use COPY unless you specifically need ADD.' },
  { id: 'df40', category: 'Docker', front: 'How do you enter a running container?', back: 'docker exec -it container_name /bin/bash (or /bin/sh if bash is not installed). -i = interactive, -t = TTY terminal. Useful for debugging but should be avoided in production (immutable containers).' },
  { id: 'df41', category: 'Docker', front: 'What is a .dockerignore file for?', back: 'Excludes files from the build context sent to the Docker daemon. Similar to .gitignore. Exclude: .git, node_modules, .env, *.md, tests. Reduces build time and context size.' },
  { id: 'df42', category: 'Docker', front: 'How do two Docker Compose containers communicate?', back: 'Services in the same docker-compose.yml share a default bridge network. They communicate via the service name as hostname. Example: from the app container, access the DB via db:5432.' },

  // ============================================================
  // === Kubernetes (~16 cards) ===
  // ============================================================
  { id: 'df43', category: 'Kubernetes', front: 'What are the Kubernetes control plane components?', back: 'kube-apiserver (API entry point), etcd (key-value database), kube-scheduler (pod placement), kube-controller-manager (reconciliation loops). The control plane runs on master nodes.' },
  { id: 'df44', category: 'Kubernetes', front: 'What is the difference between a Deployment and a StatefulSet?', back: 'Deployment: interchangeable pods (stateless), free scaling, random names. StatefulSet: stable identity (pod-0, pod-1), unique persistent storage per pod, ordered deployment. For databases.' },
  { id: 'df45', category: 'Kubernetes', front: 'What are the three Kubernetes Service types?', back: 'ClusterIP (default, accessible only within the cluster), NodePort (exposes a port on each node, 30000-32767), LoadBalancer (provisions an external cloud LB). ExternalName also exists (DNS alias).' },
  { id: 'df46', category: 'Kubernetes', front: 'What is an Ingress in Kubernetes?', back: 'Manages external HTTP/HTTPS routing to internal Services. Supports: path-based routing (/api -> service-api), host-based routing (app.example.com), TLS termination, rate limiting. Requires an Ingress Controller.' },
  { id: 'df47', category: 'Kubernetes', front: 'How does the HPA (Horizontal Pod Autoscaler) work?', back: 'The HPA monitors metrics (CPU, memory, custom) via the metrics-server and adjusts the number of replicas between min and max. Formula: desired replicas = ceil(current metrics / target metrics).' },
  { id: 'df48', category: 'Kubernetes', front: 'What is the difference between a ConfigMap and a Secret?', back: 'ConfigMap: non-sensitive configuration data (key-value or files). Secret: sensitive data encoded in base64 (passwords, tokens). Both can be injected as environment variables or mounted files.' },
  { id: 'df49', category: 'Kubernetes', front: 'What is a Helm Chart?', back: 'A Kubernetes package containing YAML templates + a values.yaml file. Allows parameterizing and deploying complex applications. helm install/upgrade/rollback. Registry: Artifact Hub.' },
  { id: 'df50', category: 'Kubernetes', front: 'How does Kubernetes achieve self-healing?', back: 'The controller manager compares the desired state (spec) to the actual state (status). If a pod crashes, the ReplicaSet recreates one. Liveness probe detects deadlocks, readiness probe removes a pod from traffic.' },
  { id: 'df51', category: 'Kubernetes', front: 'Which command displays pods with their node and IP?', back: 'kubectl get pods -o wide. Other useful commands: kubectl describe pod name (details + events), kubectl logs name (logs), kubectl exec -it name -- /bin/sh (shell into the pod).' },
  { id: 'df52', category: 'Kubernetes', front: 'What is a DaemonSet and when should you use it?', back: 'A DaemonSet ensures a pod runs on every node in the cluster. Use cases: monitoring agent (node-exporter), log collector (Fluentd/Filebeat), network plugin (Calico, Cilium).' },
  { id: 'df53', category: 'Kubernetes', front: 'How does a rolling update work in Kubernetes?', back: 'The Deployment creates a new ReplicaSet and gradually increases its replicas while reducing the old ones. maxSurge and maxUnavailable control the speed. Rollback: kubectl rollout undo.' },
  { id: 'df54', category: 'Kubernetes', front: 'What is the difference between a liveness probe and a readiness probe?', back: 'Liveness: checks if the container is alive. Failure = pod restart. Readiness: checks if the pod is ready for traffic. Failure = removed from Service (no traffic). Both support HTTP, TCP, exec.' },
  { id: 'df55', category: 'Kubernetes', front: 'What are labels and selectors in Kubernetes?', back: 'Labels are key-value pairs attached to objects (app: frontend, env: prod). Selectors filter objects by labels. Services use selectors to find pods to expose.' },
  { id: 'df56', category: 'Kubernetes', front: 'What is a PersistentVolumeClaim (PVC)?', back: 'A persistent storage request by a pod. Kubernetes binds the PVC to a PersistentVolume (PV) matching the criteria (size, accessMode, storageClass). PVs can be provisioned statically or dynamically.' },
  { id: 'df57', category: 'Kubernetes', front: 'How do you limit a Kubernetes pod\'s resources?', back: 'In the container spec: resources.requests (minimum guaranteed for scheduling) and resources.limits (maximum allowed). Exceeding memory limit = OOM kill. Exceeding CPU limit = throttling.' },
  { id: 'df58', category: 'Kubernetes', front: 'What does kubectl apply -f manifest.yaml do?', back: 'Declaratively applies the YAML file configuration to the cluster. Creates the resource if it doesn\'t exist, updates it otherwise. Preferred over kubectl create (which fails if the resource exists).' },

  // ============================================================
  // === CI/CD (~12 cards) ===
  // ============================================================
  { id: 'df59', category: 'CI/CD', front: 'What is the difference between Continuous Delivery and Continuous Deployment?', back: 'Continuous Delivery: code is automatically ready for production but deployment is manual (button). Continuous Deployment: everything is automated all the way to production without human intervention.' },
  { id: 'df60', category: 'CI/CD', front: 'What are the typical stages of a CI/CD pipeline?', back: 'Checkout code -> Build -> Unit tests -> Integration tests -> Static analysis (linting, SAST) -> Build Docker image -> Push to registry -> Deploy staging -> E2E tests -> Deploy production.' },
  { id: 'df61', category: 'CI/CD', front: 'How do you define a basic GitHub Actions workflow?', back: 'A YAML file in .github/workflows/. Structure: on (event: push, pull_request), jobs, steps. Each step uses an action (actions/checkout@v4, actions/setup-node@v4) or run (shell command).' },
  { id: 'df62', category: 'CI/CD', front: 'What is the difference between Blue-Green and Canary deployments?', back: 'Blue-Green: two complete environments, switches 100% of traffic at once (instant rollback). Canary: progressive deployment (5% -> 25% -> 50% -> 100%) while monitoring metrics at each step.' },
  { id: 'df63', category: 'CI/CD', front: 'What is an artifact in a CI/CD pipeline?', back: 'The result of a build: compiled binary, Docker image, npm package, ZIP archive. Stored in an artifact repository (Docker Hub, Nexus, Artifactory, GitHub Packages) with a version tag or hash.' },
  { id: 'df64', category: 'CI/CD', front: 'How do you store secrets in GitHub Actions?', back: 'Settings > Secrets and variables > Actions > New repository secret. Accessible in the workflow via ${{ secrets.SECRET_NAME }}. Secrets are masked in logs. Never in plain text in code.' },
  { id: 'df65', category: 'CI/CD', front: 'What is a linter in a CI pipeline?', back: 'A static analysis tool that detects style errors, potential bugs, and bad practices without executing the code. Examples: ESLint (JS), Flake8 (Python), golangci-lint (Go), hadolint (Dockerfile).' },
  { id: 'df66', category: 'CI/CD', front: 'What is the difference between a GitHub-hosted and self-hosted runner?', back: 'GitHub-hosted: ephemeral VM provided by GitHub (Ubuntu, Windows, macOS), clean for each job. Self-hosted: a machine you manage, useful for specific needs (GPU, private network, performance).' },
  { id: 'df67', category: 'CI/CD', front: 'How does the .gitlab-ci.yml file work?', back: 'Defines stages (build, test, deploy) and jobs. Each job has a stage, a script, and rules/only. Predefined CI variables ($CI_COMMIT_SHA, $CI_PIPELINE_ID). Runners execute in Docker containers.' },
  { id: 'df68', category: 'CI/CD', front: 'What is a feature flag and why use it in CI/CD?', back: 'A code toggle that enables/disables a feature without redeploying. Enables: dark launching (deploy without activating), progressive rollback, A/B testing. Tools: LaunchDarkly, Unleash, Flagsmith.' },
  { id: 'df69', category: 'CI/CD', front: 'How do you implement a rollback for a failed deployment?', back: 'Kubernetes: kubectl rollout undo deployment/name. Docker: reuse the previous version tag. Pipeline: keep artifacts from previous versions. Blue-Green: switch traffic back to the old environment.' },
  { id: 'df70', category: 'CI/CD', front: 'What is GitOps and how does it differ from traditional CI/CD?', back: 'GitOps: Git is the source of truth, an operator (ArgoCD, Flux) watches the repo and syncs the cluster (pull). Traditional CI/CD: the pipeline pushes changes (push). GitOps = declarative and auditable approach.' },

  // ============================================================
  // === Cloud (~10 cards) ===
  // ============================================================
  { id: 'df71', category: 'Cloud', front: 'What are the three cloud service models (IaaS, PaaS, SaaS)?', back: 'IaaS: infrastructure (VMs, storage, network) - e.g., EC2. PaaS: platform (runtime, middleware) - e.g., Heroku. SaaS: complete application - e.g., Gmail. The higher you go, the less you manage.' },
  { id: 'df72', category: 'Cloud', front: 'What is the difference between a Region and an Availability Zone?', back: 'Region: independent geographic area (e.g., eu-west-1 = Ireland). AZ: isolated datacenter(s) within a Region (e.g., eu-west-1a, 1b, 1c). Deploying across multiple AZs ensures high availability.' },
  { id: 'df73', category: 'Cloud', front: 'What is a VPC (Virtual Private Cloud)?', back: 'An isolated virtual network in the cloud. Allows defining subnets (public/private), route tables, security groups, and gateways. Every cloud resource runs inside a VPC.' },
  { id: 'df74', category: 'Cloud', front: 'What is the Serverless model and when should you use it?', back: 'The cloud executes code on demand without any server to manage. Billing per execution (no cost at rest). Ideal for: lightweight APIs, event-driven processing, cron jobs. Limits: cold start, execution duration.' },
  { id: 'df75', category: 'Cloud', front: 'What are the main AWS storage services?', back: 'S3: object storage (files, backups, static). EBS: block storage for EC2 (like a hard drive). EFS: shared file system (NFS). Glacier: long-term archival (very low cost, slow access).' },
  { id: 'df76', category: 'Cloud', front: 'What is the difference between a Security Group and a NACL on AWS?', back: 'Security Group: stateful firewall at the instance level (allow-only rules, automatic return). NACL: stateless firewall at the subnet level (allow/deny rules, explicit return). SG = more common.' },
  { id: 'df77', category: 'Cloud', front: 'What is AWS IAM?', back: 'Identity and Access Management. Users (humans), Roles (services/applications), Policies (JSON permissions). Principle of least privilege: grant only the permissions strictly necessary.' },
  { id: 'df78', category: 'Cloud', front: 'What is the difference between vertical and horizontal scaling?', back: 'Vertical (scale up): increase a machine\'s resources (more CPU/RAM). Physical limit. Horizontal (scale out): add identical machines. Requires a load balancer. The cloud favors horizontal.' },
  { id: 'df79', category: 'Cloud', front: 'What is CloudFormation and how does it differ from Terraform?', back: 'CloudFormation: AWS-native IaC (JSON/YAML), supports only AWS. Terraform: multi-cloud IaC (HCL), supports AWS/GCP/Azure/more. Terraform is more flexible; CloudFormation is better integrated with AWS.' },
  { id: 'df80', category: 'Cloud', front: 'What is a CDN (Content Delivery Network)?', back: 'Caches static content (images, JS, CSS) on edge servers close to users. Reduces latency and load on the origin server. Examples: CloudFront (AWS), Cloudflare, Akamai.' },

  // ============================================================
  // === IaC (~8 cards) ===
  // ============================================================
  { id: 'df81', category: 'IaC', front: 'What is the basic Terraform workflow?', back: 'terraform init (download providers), terraform plan (preview changes), terraform apply (apply changes), terraform destroy (delete everything). The state file tracks created resources.' },
  { id: 'df82', category: 'IaC', front: 'What is the terraform.tfstate file for?', back: 'Maps resources declared in HCL code to actual resources in the cloud. Terraform\'s source of truth. In teams, store in a remote backend (S3 + DynamoDB for locking).' },
  { id: 'df83', category: 'IaC', front: 'What is a Terraform module?', back: 'A reusable set of Terraform resources. Accepts variables (input) and exposes outputs. Allows factoring code (DRY). Example: a VPC module reused for each environment (dev, staging, prod).' },
  { id: 'df84', category: 'IaC', front: 'What is the difference between Ansible and Terraform?', back: 'Terraform: provisions infrastructure (create VMs, VPCs, LBs) declaratively. Ansible: configures servers (install packages, copy files) procedurally. Often used together.' },
  { id: 'df85', category: 'IaC', front: 'What is idempotency in IaC?', back: 'An operation is idempotent if it produces the same result whether executed once or N times. terraform apply or ansible-playbook only make changes if the current state differs from the desired state.' },
  { id: 'df86', category: 'IaC', front: 'How does an Ansible Playbook work?', back: 'A YAML file with plays targeting a group of servers (hosts). Each play contains tasks executed in order via modules (apt, copy, service, template). Ansible connects via SSH (agentless).' },
  { id: 'df87', category: 'IaC', front: 'What is Pulumi and how does it differ from Terraform?', back: 'Pulumi is an IaC tool that uses real programming languages (TypeScript, Python, Go) instead of HCL. Allows native loops, conditionals, and tests. Multi-cloud like Terraform.' },
  { id: 'df88', category: 'IaC', front: 'Why version your infrastructure (Infrastructure as Code)?', back: 'Reproducibility (recreate an identical env), traceability (who changed what, when), review (PRs on infra changes), rollback (revert to a previous version), living documentation of the system.' },

  // ============================================================
  // === Monitoring (~6 cards) ===
  // ============================================================
  { id: 'df89', category: 'Monitoring', front: 'What are the three pillars of observability?', back: 'Metrics (time-series numerical data: CPU, latency, requests/s), Logs (timestamped textual events), Traces (tracking a request\'s path through microservices). Tools: Prometheus + Loki + Jaeger.' },
  { id: 'df90', category: 'Monitoring', front: 'What are the 4 Golden Signals of monitoring?', back: 'Latency (response time), Traffic (number of requests), Errors (failure rate), Saturation (resource utilization). Defined by the Google SRE Book. The priority metrics to monitor for any service.' },
  { id: 'df91', category: 'Monitoring', front: 'How does Prometheus work?', back: 'Prometheus scrapes (pulls) metrics from /metrics endpoints of services at regular intervals. Local time-series storage. PromQL for queries. Alertmanager for alerts. Grafana for visualization.' },
  { id: 'df92', category: 'Monitoring', front: 'What is the ELK Stack for?', back: 'Elasticsearch (log indexing and search), Logstash (collection and transformation), Kibana (visualization and dashboards). Alternative: PLG Stack (Promtail + Loki + Grafana), lighter for logs.' },
  { id: 'df93', category: 'Monitoring', front: 'What is distributed tracing?', back: 'Tracking a request through all microservices. Each service adds a span (unit of work) to the trace (identified by a trace-id). Helps identify bottlenecks. Tools: Jaeger, Zipkin, OTel.' },
  { id: 'df94', category: 'Monitoring', front: 'How do you configure a Prometheus alert?', back: 'Define a rule in prometheus.rules.yml with a PromQL expression (e.g., rate(http_errors[5m]) > 0.05), a for (duration before triggering), labels, and annotations. Alertmanager routes to Slack, email, PagerDuty.' },

  // ============================================================
  // === Security (~4 cards) ===
  // ============================================================
  { id: 'df95', category: 'Securite', front: 'What is "shift left" in DevOps security?', back: 'Integrating security testing as early as possible in the dev cycle (IDE, CI) rather than in production. SAST (static code analysis), SCA (dependency scanning), secret scanning in the CI pipeline.' },
  { id: 'df96', category: 'Securite', front: 'How do you manage secrets in a DevOps environment?', back: 'Never commit in plain text to Git. Solutions: HashiCorp Vault (centralized), Kubernetes Secrets (base64), AWS Secrets Manager, encrypted CI/CD variables. Regular rotation and access auditing.' },
  { id: 'df97', category: 'Securite', front: 'What is the Zero Trust model?', back: '"Never trust, always verify." Every request is authenticated/authorized, whether internal or external. Network microsegmentation, mutual authentication (mTLS), least privilege, continuous verification.' },
  { id: 'df98', category: 'Securite', front: 'What are Network Policies in Kubernetes?', back: 'Firewall rules at the pod level (L3/L4). Define allowed ingress/egress traffic by labels and namespaces. By default, everything is allowed. Requires a compatible CNI (Calico, Cilium). Principle: deny all, allow specific.' },

  // ============================================================
  // === SRE (~3 cards) ===
  // ============================================================
  { id: 'df99', category: 'SRE', front: 'What is the difference between SLI, SLO, and SLA?', back: 'SLI: measured metric (e.g., 99.95% of requests < 200ms). SLO: internal target (e.g., 99.9% availability). SLA: customer contract with penalties (e.g., 99.5% guaranteed). SLI measures, SLO targets, SLA commits.' },
  { id: 'df100', category: 'SRE', front: 'What is an Error Budget and how do you use it?', back: 'Error budget = 100% - SLO. For a 99.9% SLO, 43.8 min of downtime/month is tolerated. Budget remaining = you can deploy. Budget exhausted = release freeze, reliability takes priority.' },
  { id: 'df101', category: 'SRE', front: 'What is a blameless post-mortem?', back: 'An incident analysis without blaming individuals. Structure: timeline, detection, resolution, impact, root causes (5 Whys), corrective actions. Goal: improve systems and processes, not punish individuals.' },
]
