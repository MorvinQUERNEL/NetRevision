import type { QuizQuestion } from '../quizzes'

export const quizzes: Record<number, QuizQuestion[]> = {
  119: [
    { id: 'q119_1', question: 'Quel est le role de l\'API Server dans l\'architecture Kubernetes ?', options: ['Il stocke l\'etat du cluster', 'C\'est le point d\'entree central qui recoit toutes les requetes et expose l\'API REST de Kubernetes', 'Il planifie les pods sur les noeuds', 'Il gere les conteneurs sur les noeuds worker'], correct: 1, explanation: 'L\'API Server (kube-apiserver) est le composant central du control plane. Toutes les interactions (kubectl, dashboards, controleurs) passent par lui. Il valide les requetes, les persiste dans etcd et notifie les composants concernes.' },
    { id: 'q119_2', question: 'Quel composant Kubernetes stocke l\'etat complet du cluster de maniere persistante ?', options: ['kube-scheduler', 'etcd', 'kube-controller-manager', 'kubelet'], correct: 1, explanation: 'etcd est un store cle-valeur distribue et hautement disponible qui stocke toutes les donnees de configuration et l\'etat du cluster Kubernetes. Seul l\'API Server communique directement avec etcd. Les backups etcd sont essentiels pour la reprise apres sinistre.' },
    { id: 'q119_3', question: 'Quel composant decide sur quel noeud un pod doit etre planifie ?', options: ['kubelet', 'kube-proxy', 'kube-scheduler', 'kube-controller-manager'], correct: 2, explanation: 'Le kube-scheduler surveille les pods non assignes et les planifie sur des noeuds en fonction des ressources disponibles, des contraintes (affinite, tolerations, taints), des limites de ressources et des politiques de placement.' },
    { id: 'q119_4', question: 'Quel agent s\'execute sur chaque noeud worker pour gerer les pods et communiquer avec le control plane ?', options: ['kube-proxy', 'kubelet', 'containerd', 'kube-scheduler'], correct: 1, explanation: 'Le kubelet est l\'agent qui s\'execute sur chaque noeud worker. Il recoit les specifications de pods de l\'API Server, demande au container runtime (containerd, CRI-O) de demarrer les conteneurs, et rapporte l\'etat des pods au control plane.' },
    { id: 'q119_5', question: 'Quel composant Kubernetes gere les regles reseau pour le routage du trafic vers les Services ?', options: ['kubelet', 'kube-proxy', 'kube-scheduler', 'CoreDNS'], correct: 1, explanation: 'kube-proxy s\'execute sur chaque noeud et maintient les regles reseau (iptables ou IPVS) qui permettent de router le trafic vers les pods backend d\'un Service Kubernetes. Il gere le load balancing interne au cluster.' },
    { id: 'q119_6', question: 'Quelle commande affiche les noeuds du cluster Kubernetes ?', options: ['kubectl list nodes', 'kubectl get nodes', 'kubectl show nodes', 'kubectl describe cluster'], correct: 1, explanation: '"kubectl get nodes" affiche la liste des noeuds avec leur statut (Ready/NotReady), leur role et leur version. "kubectl describe node <nom>" donne des details supplementaires sur les ressources, les taints et les conditions.' },
    { id: 'q119_7', question: 'Quel est le role du kube-controller-manager ?', options: ['Il gere les DNS du cluster', 'Il execute les boucles de controle qui maintiennent l\'etat desire du cluster (ReplicaSet, Deployment, Node, etc.)', 'Il stocke les configurations', 'Il gere l\'authentification des utilisateurs'], correct: 1, explanation: 'Le kube-controller-manager regroupe les controleurs internes (Deployment, ReplicaSet, Node, Job, ServiceAccount). Chaque controleur surveille l\'etat actuel via l\'API Server et agit pour le rapprocher de l\'etat desire (boucle de reconciliation).' },
    { id: 'q119_8', question: 'Quel outil local permet de creer un cluster Kubernetes a un seul noeud pour le developpement ?', options: ['kubeadm', 'minikube', 'kops', 'Rancher'], correct: 1, explanation: 'minikube cree un cluster Kubernetes local a un seul noeud (dans une VM ou un conteneur). Ideal pour le developpement et l\'apprentissage. D\'autres alternatives locales existent : kind (Kubernetes in Docker), k3d (k3s in Docker), Docker Desktop.' },
    { id: 'q119_9', question: 'Quel est le runtine de conteneurs par defaut dans les versions recentes de Kubernetes ?', options: ['Docker', 'containerd', 'rkt', 'LXC'], correct: 1, explanation: 'Depuis Kubernetes 1.24, le support direct de Docker (dockershim) a ete supprime. containerd est le runtime de conteneurs par defaut, conforme au CRI (Container Runtime Interface). CRI-O est une autre alternative populaire.' },
    { id: 'q119_10', question: 'Quelle commande kubectl affiche les details complets d\'un pod incluant les evenements ?', options: ['kubectl get pod mon-pod -o yaml', 'kubectl describe pod mon-pod', 'kubectl inspect pod mon-pod', 'kubectl logs pod mon-pod'], correct: 1, explanation: '"kubectl describe pod mon-pod" affiche des details lisibles : spec, status, conditions, volumes, events. Les events en bas sont precieux pour le debug (pull d\'image, erreur de scheduling, OOMKilled, etc.).' }
  ],
  120: [
    { id: 'q120_1', question: 'Quelle est la plus petite unite deployable dans Kubernetes ?', options: ['Conteneur', 'Pod', 'Deployment', 'ReplicaSet'], correct: 1, explanation: 'Le Pod est la plus petite unite deployable dans Kubernetes. Il contient un ou plusieurs conteneurs qui partagent le meme espace de noms reseau (meme IP), les memes volumes et le meme cycle de vie. La plupart des pods n\'ont qu\'un seul conteneur.' },
    { id: 'q120_2', question: 'Quel est le role d\'un Deployment dans Kubernetes ?', options: ['Il cree un seul pod', 'Il gere le deploiement declaratif des pods, les mises a jour et le rollback via un ReplicaSet', 'Il expose les pods au reseau', 'Il stocke les configurations'], correct: 1, explanation: 'Un Deployment gere un ReplicaSet qui gere les pods. Il permet les mises a jour progressives (rolling update), le rollback vers des versions precedentes et le scaling. C\'est l\'objet le plus utilise pour deployer des applications stateless.' },
    { id: 'q120_3', question: 'Quelle commande cree un Deployment avec 3 replicas d\'une image nginx ?', options: ['kubectl run nginx --replicas=3', 'kubectl create deployment nginx --image=nginx --replicas=3', 'kubectl deploy nginx --image=nginx -r 3', 'kubectl new deployment nginx 3'], correct: 1, explanation: '"kubectl create deployment nginx --image=nginx --replicas=3" cree un Deployment avec 3 pods nginx. On peut aussi utiliser un fichier YAML avec "kubectl apply -f deployment.yaml" pour une configuration declarative (recommande).' },
    { id: 'q120_4', question: 'Quel type de Service Kubernetes expose des pods a l\'interieur du cluster uniquement ?', options: ['NodePort', 'ClusterIP', 'LoadBalancer', 'ExternalName'], correct: 1, explanation: 'ClusterIP (type par defaut) cree une IP virtuelle accessible uniquement a l\'interieur du cluster. Les autres pods peuvent joindre le service par son nom DNS (nom-service.namespace.svc.cluster.local). Ideal pour les communications internes.' },
    { id: 'q120_5', question: 'Quel type de Service expose un port sur chaque noeud du cluster ?', options: ['ClusterIP', 'NodePort', 'LoadBalancer', 'Ingress'], correct: 1, explanation: 'NodePort ouvre un port (30000-32767) sur chaque noeud du cluster. Le trafic vers nodeIP:nodePort est route vers le Service puis vers les pods. C\'est utile pour le developpement ou quand on n\'a pas de load balancer cloud.' },
    { id: 'q120_6', question: 'Comment scaler un Deployment de 3 a 5 replicas ?', options: ['kubectl resize deployment nginx --replicas=5', 'kubectl scale deployment nginx --replicas=5', 'kubectl update deployment nginx --pods=5', 'kubectl set replicas deployment nginx 5'], correct: 1, explanation: '"kubectl scale deployment nginx --replicas=5" modifie le nombre de replicas en cours d\'execution. Kubernetes cree les pods supplementaires ou supprime les excedentaires. Pour persister le changement, modifier le YAML et "kubectl apply".' },
    { id: 'q120_7', question: 'Quel objet Kubernetes maintient un nombre stable de replicas de pods ?', options: ['Deployment', 'ReplicaSet', 'DaemonSet', 'StatefulSet'], correct: 1, explanation: 'Le ReplicaSet garantit qu\'un nombre specifie de replicas de pods sont en cours d\'execution a tout moment. En pratique, on utilise les Deployments qui gerent les ReplicaSets automatiquement (creation, scaling, rolling update).' },
    { id: 'q120_8', question: 'Quelle commande applique un fichier de configuration YAML a Kubernetes ?', options: ['kubectl create -f fichier.yaml', 'kubectl apply -f fichier.yaml', 'kubectl deploy -f fichier.yaml', 'kubectl run -f fichier.yaml'], correct: 1, explanation: '"kubectl apply -f" est la methode declarative recommandee. Il cree la ressource si elle n\'existe pas et la met a jour sinon. "kubectl create -f" cree mais echoue si la ressource existe deja. apply est idempotent, create non.' },
    { id: 'q120_9', question: 'Quel objet Kubernetes execute un pod sur chaque noeud du cluster ?', options: ['ReplicaSet', 'StatefulSet', 'DaemonSet', 'Job'], correct: 2, explanation: 'Un DaemonSet garantit qu\'un pod s\'execute sur chaque noeud (ou un sous-ensemble). Cas d\'usage : agents de monitoring (Prometheus Node Exporter), collecteurs de logs (Fluentd), agents de reseau (kube-proxy, Calico). Un nouveau noeud recoit automatiquement le pod.' },
    { id: 'q120_10', question: 'Quel Service Kubernetes provisionne automatiquement un load balancer cloud ?', options: ['ClusterIP', 'NodePort', 'LoadBalancer', 'ExternalName'], correct: 2, explanation: 'Le type LoadBalancer cree automatiquement un load balancer chez le cloud provider (AWS ELB, GCP Load Balancer, Azure LB) et route le trafic externe vers les pods. Il inclut un NodePort et un ClusterIP automatiquement.' }
  ],
  121: [
    { id: 'q121_1', question: 'Quel est le role d\'un Ingress dans Kubernetes ?', options: ['Il stocke les donnees des pods', 'Il gere le routage HTTP/HTTPS externe vers les Services internes du cluster', 'Il planifie les pods sur les noeuds', 'Il gere les volumes persistants'], correct: 1, explanation: 'L\'Ingress expose des routes HTTP/HTTPS depuis l\'exterieur du cluster vers des Services internes. Il permet le routage base sur l\'hote ou le chemin, la terminaison TLS et le virtual hosting. Il necessite un Ingress Controller (Nginx, Traefik, HAProxy).' },
    { id: 'q121_2', question: 'Quel composant doit etre installe pour que les ressources Ingress fonctionnent ?', options: ['kube-proxy', 'Un Ingress Controller (ex: nginx-ingress, traefik)', 'CoreDNS', 'etcd'], correct: 1, explanation: 'Un Ingress Controller est un pod qui implemente les regles Ingress. Les plus populaires sont Nginx Ingress Controller, Traefik et HAProxy. Sans Ingress Controller, les ressources Ingress n\'ont aucun effet.' },
    { id: 'q121_3', question: 'Quelle annotation Ingress configure la redirection HTTP vers HTTPS ?', options: ['nginx.ingress.kubernetes.io/ssl-redirect: "true"', 'ingress.kubernetes.io/force-https: "true"', 'kubernetes.io/redirect-https: "true"', 'nginx.ingress.kubernetes.io/https-only: "true"'], correct: 0, explanation: 'L\'annotation "nginx.ingress.kubernetes.io/ssl-redirect: true" force la redirection du trafic HTTP vers HTTPS. Chaque Ingress Controller a ses propres annotations. Traefik utilise par exemple traefik.ingress.kubernetes.io/.' },
    { id: 'q121_4', question: 'Comment configurer le routage base sur le nom d\'hote dans un Ingress ?', options: ['Via l\'option --host dans kubectl', 'Via la spec.rules[].host dans le manifest YAML de l\'Ingress', 'Via une annotation host-routing', 'Via un ConfigMap'], correct: 1, explanation: 'Le champ spec.rules[].host definit le nom d\'hote (ex: app.example.com). Plusieurs regles host peuvent coexister pour le virtual hosting. Les requetes sans Host header correspondent a la regle sans host (default backend).' },
    { id: 'q121_5', question: 'Comment configurer le TLS (HTTPS) sur un Ingress ?', options: ['Le TLS est automatique', 'Via la section spec.tls avec un Secret contenant le certificat et la cle privee', 'Via une annotation tls-enable', 'En installant un plugin TLS'], correct: 1, explanation: 'La section spec.tls reference un Secret Kubernetes de type kubernetes.io/tls contenant les champs tls.crt (certificat) et tls.key (cle privee). cert-manager peut automatiser la gestion des certificats Let\'s Encrypt.' },
    { id: 'q121_6', question: 'Quel outil automatise l\'obtention et le renouvellement des certificats TLS dans Kubernetes ?', options: ['kubectl-tls', 'cert-manager', 'ssl-manager', 'kube-ssl'], correct: 1, explanation: 'cert-manager est un controleur Kubernetes qui automatise la gestion des certificats TLS. Il supporte Let\'s Encrypt (ACME), Vault, et d\'autres issuers. Il cree et renouvelle automatiquement les certificats sous forme de Secrets.' },
    { id: 'q121_7', question: 'Quelle regle Ingress route le trafic vers differents services selon le chemin URL ?', options: ['hostRules', 'pathType et path dans spec.rules[].http.paths[]', 'urlMapping', 'routeConfig'], correct: 1, explanation: 'spec.rules[].http.paths[] definit les chemins et les services cibles. pathType peut etre Prefix (/api/* vers service-api), Exact (/login vers service-auth) ou ImplementationSpecific. Chaque chemin pointe vers un service.name et service.port.' },
    { id: 'q121_8', question: 'Que se passe-t-il quand une requete ne correspond a aucune regle Ingress ?', options: ['La requete est rejetee avec 403', 'Elle est envoyee au defaultBackend si configure, sinon 404', 'Elle est routee vers le premier service', 'Elle est mise en file d\'attente'], correct: 1, explanation: 'Si aucune regle ne correspond, l\'Ingress Controller utilise le defaultBackend (configure dans l\'Ingress ou dans la config du controller). Sans defaultBackend, la plupart des controllers renvoient une page 404.' },
    { id: 'q121_9', question: 'Quelle annotation Nginx Ingress limite le taux de requetes (rate limiting) ?', options: ['nginx.ingress.kubernetes.io/rate-limit: "100"', 'nginx.ingress.kubernetes.io/limit-rps: "10"', 'nginx.ingress.kubernetes.io/throttle: "100"', 'nginx.ingress.kubernetes.io/max-requests: "100"'], correct: 1, explanation: 'L\'annotation "limit-rps" (requests per second) limite le nombre de requetes par seconde depuis une meme IP. D\'autres annotations utiles : limit-connections, limit-rpm, whitelist-source-range pour le controle d\'acces.' },
    { id: 'q121_10', question: 'Quel est l\'avantage d\'un Ingress par rapport a plusieurs Services de type LoadBalancer ?', options: ['L\'Ingress est plus rapide', 'Un seul point d\'entree (IP/Load Balancer) pour plusieurs services, reduisant les couts et simplifiant la gestion DNS', 'L\'Ingress supporte plus de protocoles', 'Il n\'y a pas d\'avantage'], correct: 1, explanation: 'Un Ingress utilise un seul Load Balancer externe pour router le trafic vers plusieurs services internes (via host/path). Chaque Service LoadBalancer cree un LB cloud dedie, ce qui multiplie les couts et les IPs publiques a gerer.' }
  ],
  122: [
    { id: 'q122_1', question: 'Qu\'est-ce que Helm dans l\'ecosysteme Kubernetes ?', options: ['Un outil de monitoring', 'Un gestionnaire de packages (charts) pour deployer des applications sur Kubernetes', 'Un scanner de securite', 'Un outil de backup'], correct: 1, explanation: 'Helm est le gestionnaire de packages de Kubernetes. Un "chart" Helm est un ensemble de templates YAML parametrables qui definissent une application complete. Il simplifie le deploiement, la mise a jour et le rollback d\'applications complexes.' },
    { id: 'q122_2', question: 'Quelle commande Helm installe un chart depuis un repository ?', options: ['helm deploy stable/nginx', 'helm install mon-release stable/nginx', 'helm create mon-release stable/nginx', 'helm run stable/nginx'], correct: 1, explanation: '"helm install mon-release stable/nginx" installe le chart nginx avec le nom de release "mon-release". La release est une instance du chart deployee. On peut personnaliser avec --values fichier.yaml ou --set key=value.' },
    { id: 'q122_3', question: 'Quel fichier contient les valeurs par defaut d\'un chart Helm ?', options: ['Chart.yaml', 'values.yaml', 'config.yaml', 'defaults.yaml'], correct: 1, explanation: 'values.yaml contient les valeurs par defaut utilisees par les templates du chart. Chart.yaml contient les metadonnees (nom, version, description). Les utilisateurs peuvent surcharger les valeurs avec "helm install --values custom-values.yaml".' },
    { id: 'q122_4', question: 'Quelle commande Helm met a jour une release existante avec de nouvelles valeurs ?', options: ['helm update mon-release', 'helm upgrade mon-release mon-chart', 'helm modify mon-release', 'helm redeploy mon-release'], correct: 1, explanation: '"helm upgrade mon-release mon-chart" met a jour la release avec les nouvelles valeurs ou une nouvelle version du chart. Helm conserve l\'historique des revisions, permettant un rollback en cas de probleme.' },
    { id: 'q122_5', question: 'Quelle commande Helm revient a la version precedente d\'une release ?', options: ['helm undo mon-release', 'helm rollback mon-release', 'helm revert mon-release', 'helm restore mon-release'], correct: 1, explanation: '"helm rollback mon-release [revision]" restaure une revision precedente. "helm history mon-release" affiche l\'historique des revisions. Sans numero de revision, rollback restaure la revision immediatement precedente.' },
    { id: 'q122_6', question: 'Qu\'est-ce qu\'un template Helm ?', options: ['Un fichier YAML statique', 'Un fichier YAML avec des directives Go template ({{ .Values.x }}) qui sont rendus dynamiquement', 'Un fichier JSON de configuration', 'Un script shell de deploiement'], correct: 1, explanation: 'Les templates Helm sont des fichiers YAML utilisant la syntaxe Go template ({{ .Values.replicaCount }}, {{ if }}, {{ range }}). Ils sont rendus dynamiquement avec les valeurs de values.yaml, permettant de generer des manifests Kubernetes personnalises.' },
    { id: 'q122_7', question: 'Quelle commande Helm genere les manifests YAML sans les deployer ?', options: ['helm render mon-chart', 'helm template mon-release mon-chart', 'helm dry-run mon-chart', 'helm generate mon-chart'], correct: 1, explanation: '"helm template mon-release mon-chart" genere les manifests YAML a partir des templates sans communiquer avec le cluster. C\'est utile pour la revue, le debug et l\'integration dans des pipelines CI/CD (GitOps).' },
    { id: 'q122_8', question: 'Ou sont stockes les charts Helm publics ?', options: ['Docker Hub', 'GitHub uniquement', 'Des repositories Helm (Artifact Hub, registres OCI)', 'npm registry'], correct: 2, explanation: 'Artifact Hub (artifacthub.io) est le catalogue central des charts Helm publics. Les charts peuvent aussi etre heberges dans des registres OCI (comme les images Docker), des repos HTTP custom, ou des repos Git. "helm repo add" ajoute un repository.' },
    { id: 'q122_9', question: 'Quelle commande Helm liste toutes les releases deployees dans le namespace courant ?', options: ['helm show releases', 'helm list', 'helm deployed', 'helm releases'], correct: 1, explanation: '"helm list" (ou "helm ls") affiche toutes les releases dans le namespace courant avec leur statut, revision, chart et date de deploiement. "--all-namespaces" (ou "-A") liste les releases de tous les namespaces.' },
    { id: 'q122_10', question: 'Quel fichier Helm definit les dependances du chart (sous-charts) ?', options: ['dependencies.yaml', 'Chart.yaml (section dependencies)', 'requirements.yaml', 'subchart.yaml'], correct: 1, explanation: 'Depuis Helm 3, les dependances sont definies dans la section "dependencies" de Chart.yaml (anciennement requirements.yaml dans Helm 2). "helm dependency update" telecharge les sous-charts dans le repertoire charts/.' }
  ],
  123: [
    { id: 'q123_1', question: 'Quel objet Kubernetes stocke des donnees sensibles comme des mots de passe ou des tokens ?', options: ['ConfigMap', 'Secret', 'Volume', 'Annotation'], correct: 1, explanation: 'Les Secrets stockent des donnees sensibles (mots de passe, tokens, cles SSH) en base64 (pas chiffre par defaut !). Ils peuvent etre montes en volumes ou injectes en variables d\'environnement. Pour le chiffrement au repos, configurer EncryptionConfiguration ou utiliser un KMS.' },
    { id: 'q123_2', question: 'Quelle est la difference entre un Secret et un ConfigMap ?', options: ['Il n\'y a pas de difference', 'Un Secret stocke des donnees sensibles encodees en base64, un ConfigMap stocke des donnees de configuration non sensibles en clair', 'Un ConfigMap est plus securise qu\'un Secret', 'Un Secret est un ConfigMap chiffre'], correct: 1, explanation: 'Les ConfigMaps stockent des configurations non sensibles (fichiers .conf, variables). Les Secrets sont similaires mais encodent les donnees en base64 et ont des politiques d\'acces RBAC separees. Attention : base64 n\'est PAS du chiffrement.' },
    { id: 'q123_3', question: 'Comment injecter un Secret comme variable d\'environnement dans un pod ?', options: ['Via une annotation sur le pod', 'Via env[].valueFrom.secretKeyRef dans la spec du conteneur', 'Via un fichier .env dans l\'image', 'Via un label sur le pod'], correct: 1, explanation: 'La spec du conteneur utilise env[].valueFrom.secretKeyRef avec le nom du Secret et la cle. Alternativement, envFrom[].secretRef injecte toutes les cles du Secret comme variables. Le Secret doit exister dans le meme namespace que le pod.' },
    { id: 'q123_4', question: 'Quelle commande cree un Secret a partir d\'un fichier ?', options: ['kubectl create secret file mon-secret --from-file=./config', 'kubectl create secret generic mon-secret --from-file=./config', 'kubectl secret create mon-secret ./config', 'kubectl add secret mon-secret --file=./config'], correct: 1, explanation: '"kubectl create secret generic" cree un Secret de type Opaque. --from-file cree une cle par fichier (nom du fichier = cle, contenu = valeur). --from-literal=cle=valeur pour des valeurs individuelles. --type=kubernetes.io/tls pour les certificats.' },
    { id: 'q123_5', question: 'Quel outil externe peut gerer les Secrets Kubernetes de maniere securisee avec un vault ?', options: ['kubectl-secrets', 'HashiCorp Vault avec le Vault Agent Injector ou External Secrets Operator', 'Docker Secrets', 'git-crypt'], correct: 1, explanation: 'HashiCorp Vault est un gestionnaire de secrets centralise. Le Vault Agent Injector injecte les secrets dans les pods via des annotations. External Secrets Operator synchronise les secrets depuis Vault, AWS Secrets Manager, Azure Key Vault vers des Secrets Kubernetes.' },
    { id: 'q123_6', question: 'Comment monter un Secret comme fichier dans un pod ?', options: ['Via un bind mount', 'Via un volume de type secret dans la spec du pod et un volumeMount dans le conteneur', 'Via une annotation mount-secret', 'Via un ConfigMap special'], correct: 1, explanation: 'On declare un volume de type "secret" (secretName: mon-secret) dans spec.volumes, puis on le monte avec volumeMounts dans le conteneur. Chaque cle du Secret devient un fichier dans le repertoire de montage.' },
    { id: 'q123_7', question: 'Quelle commande affiche le contenu decode d\'un Secret Kubernetes ?', options: ['kubectl get secret mon-secret -o yaml', 'kubectl get secret mon-secret -o jsonpath=\'{.data.password}\' | base64 --decode', 'kubectl describe secret mon-secret --decode', 'kubectl show secret mon-secret'], correct: 1, explanation: 'Les valeurs des Secrets sont stockees en base64. "kubectl get secret -o jsonpath" extrait une cle specifique, et "base64 --decode" la decode. "kubectl get secret -o yaml" affiche tout mais en base64. "describe" ne montre que les tailles.' },
    { id: 'q123_8', question: 'Que permet Sealed Secrets dans Kubernetes ?', options: ['De chiffrer les Secrets pour pouvoir les versionner en toute securite dans Git', 'De compresser les Secrets', 'De supprimer les Secrets automatiquement', 'De partager les Secrets entre clusters'], correct: 0, explanation: 'Sealed Secrets (Bitnami) permet de chiffrer les Secrets avec une cle publique pour les stocker en toute securite dans Git. Le controleur Sealed Secrets dans le cluster dechiffre le SealedSecret en Secret Kubernetes classique avec sa cle privee.' },
    { id: 'q123_9', question: 'Comment limiter l\'acces aux Secrets a certains pods ou utilisateurs ?', options: ['En renommant les Secrets', 'Via les politiques RBAC (Role/ClusterRole avec des regles sur les Secrets)', 'En les chiffrant en AES', 'En utilisant des labels'], correct: 1, explanation: 'Le RBAC (Role-Based Access Control) de Kubernetes permet de creer des Roles qui autorisent ou interdisent l\'acces aux Secrets par namespace. On lie les Roles aux ServiceAccounts ou utilisateurs via des RoleBindings. Le principe du moindre privilege est essentiel.' },
    { id: 'q123_10', question: 'Quelle configuration Kubernetes chiffre les Secrets au repos dans etcd ?', options: ['SecretEncryption: true dans kubelet', 'EncryptionConfiguration avec un provider aescbc ou kms', 'TLS sur etcd', 'RBAC sur les Secrets'], correct: 1, explanation: 'L\'EncryptionConfiguration de l\'API Server definit comment les Secrets sont chiffres avant d\'etre stockes dans etcd. Les providers incluent aescbc (AES-CBC), secretbox, et kms (AWS KMS, Azure Key Vault, HashiCorp Vault). Par defaut, les Secrets ne sont PAS chiffres dans etcd.' }
  ],
  124: [
    { id: 'q124_1', question: 'Quel objet Kubernetes ajuste automatiquement le nombre de replicas d\'un Deployment en fonction de la charge ?', options: ['ReplicaSet', 'HorizontalPodAutoscaler (HPA)', 'DaemonSet', 'StatefulSet'], correct: 1, explanation: 'Le HPA surveille des metriques (CPU, memoire, metriques custom) et ajuste automatiquement le nombre de replicas d\'un Deployment ou StatefulSet. Il necessite le Metrics Server pour les metriques de base (CPU/memoire).' },
    { id: 'q124_2', question: 'Quelle commande cree un HPA qui maintient l\'utilisation CPU a 50% avec un maximum de 10 pods ?', options: ['kubectl autoscale deployment nginx --max=10 --cpu=50', 'kubectl autoscale deployment nginx --cpu-percent=50 --min=1 --max=10', 'kubectl scale deployment nginx --auto --cpu=50%', 'kubectl hpa create nginx --target-cpu=50 --max=10'], correct: 1, explanation: '"kubectl autoscale deployment nginx --cpu-percent=50 --min=1 --max=10" cree un HPA qui maintient l\'utilisation CPU moyenne des pods a 50%. Si la charge augmente, le HPA cree des pods (jusqu\'a 10). Si elle diminue, il en supprime (minimum 1).' },
    { id: 'q124_3', question: 'Quel composant doit etre installe pour que le HPA puisse collecter les metriques CPU et memoire ?', options: ['Prometheus', 'Metrics Server', 'Grafana', 'cAdvisor'], correct: 1, explanation: 'Le Metrics Server collecte les metriques de ressources (CPU, memoire) de chaque pod via le kubelet/cAdvisor et les expose via l\'API Metrics. Le HPA interroge cette API pour prendre ses decisions de scaling. Prometheus est utilise pour les metriques custom.' },
    { id: 'q124_4', question: 'Quelle est la difference entre le HPA (Horizontal Pod Autoscaler) et le VPA (Vertical Pod Autoscaler) ?', options: ['Il n\'y a pas de difference', 'Le HPA ajuste le nombre de replicas, le VPA ajuste les ressources (CPU/memoire) de chaque pod', 'Le VPA est plus rapide que le HPA', 'Le HPA fonctionne uniquement avec CPU, le VPA avec la memoire'], correct: 1, explanation: 'Le HPA scale horizontalement en ajoutant/supprimant des pods. Le VPA scale verticalement en modifiant les requests/limits de CPU et memoire de chaque pod (necessite generalement un redemarrage). Ils ne doivent pas etre utilises ensemble sur la meme metrique.' },
    { id: 'q124_5', question: 'Que sont les resource requests et limits dans Kubernetes ?', options: ['Des labels pour organiser les pods', 'requests = ressources garanties au pod, limits = maximum autorise avant throttling/kill', 'Des annotations de monitoring', 'Des regles RBAC'], correct: 1, explanation: 'Les requests definissent les ressources minimales garanties (utilisees pour le scheduling). Les limits definissent le maximum : si un pod depasse sa limit CPU, il est throttle ; s\'il depasse sa limit memoire, il est tue (OOMKilled). Toujours definir les deux.' },
    { id: 'q124_6', question: 'Que se passe-t-il quand un pod depasse sa limite memoire (memory limit) ?', options: ['Le pod est throttle (ralenti)', 'Le pod est tue par l\'OOM Killer (OOMKilled) et redemarre selon la restartPolicy', 'Rien, les limits sont indicatives', 'Le pod est migre vers un autre noeud'], correct: 1, explanation: 'Si un conteneur depasse sa memory limit, le noyau Linux declenche l\'OOM Killer qui tue le processus. Kubernetes voit le pod en etat OOMKilled et le redemarre selon la restartPolicy (Always par defaut pour les Deployments).' },
    { id: 'q124_7', question: 'Quel objet Kubernetes definit des quotas de ressources par namespace ?', options: ['LimitRange', 'ResourceQuota', 'PodSecurityPolicy', 'NetworkPolicy'], correct: 1, explanation: 'ResourceQuota limite les ressources totales consommables dans un namespace : nombre de pods, CPU/memoire totales, nombre de Services, PVCs, etc. LimitRange definit des valeurs par defaut et des limites min/max par pod/conteneur.' },
    { id: 'q124_8', question: 'Quelle est la difference entre le Cluster Autoscaler et le HPA ?', options: ['Il n\'y a pas de difference', 'Le HPA ajuste le nombre de pods, le Cluster Autoscaler ajuste le nombre de noeuds du cluster', 'Le Cluster Autoscaler est plus rapide', 'Le HPA gere les noeuds'], correct: 1, explanation: 'Le HPA ajoute des pods mais si les noeuds existants n\'ont pas assez de ressources, les pods restent en Pending. Le Cluster Autoscaler detecte ces pods Pending et ajoute des noeuds au cluster (via l\'API du cloud provider). Ils fonctionnent ensemble.' },
    { id: 'q124_9', question: 'Quelle metrique custom peut etre utilisee par le HPA en plus de CPU et memoire ?', options: ['Uniquement CPU et memoire', 'Des metriques custom exposees via l\'API custom.metrics.k8s.io (ex: requetes/seconde, taille de queue)', 'Uniquement des metriques reseau', 'Des metriques Git'], correct: 1, explanation: 'Le HPA v2 supporte les metriques custom (requetes HTTP/s, messages en queue, latence) via l\'API custom.metrics.k8s.io. Un adaptateur (Prometheus Adapter, Datadog, KEDA) expose ces metriques au HPA pour un scaling base sur la charge applicative reelle.' },
    { id: 'q124_10', question: 'Quel outil etend le HPA avec des scalers event-driven (Kafka, RabbitMQ, Cron, etc.) ?', options: ['Prometheus', 'KEDA (Kubernetes Event-Driven Autoscaling)', 'Argo', 'Flux'], correct: 1, explanation: 'KEDA (Kubernetes Event-Driven Autoscaling) ajoute des scalers specialises au HPA : Kafka (nombre de messages), RabbitMQ (taille de queue), Cron (horaire), HTTP (requetes), Azure Queue, AWS SQS, etc. Il peut aussi scaler a zero replicas.' }
  ]
}
